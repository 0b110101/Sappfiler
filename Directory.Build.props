<Project>

  <!--
    全仓库统一的版本号定义。
    MSBuild 会自动向上查找本文件并应用到 src/ 与 tests/ 下的所有工程，
    所以改动版本号只需改这一处。

    ── 版本规则（SemVer 2.0.0）──────────────────────────────────────────────
    格式：  MAJOR.MINOR.PATCH[-预发布标识[.序号]][+构建信息]

    数字段（递增某段时右侧各段归零）：
      MAJOR  不兼容的改动（对外行为/接口破坏性变更）。0 → 1 表示脱离初始开发阶段。
      MINOR  向下兼容地新增功能。
      PATCH  向下兼容地修缺陷。

    初始开发阶段（0.Y.Z，当前阶段）的约定：
      · **正式确认可用的版本**：`0.2.20`、`0.2.21` …… 每个交付包递增 PATCH。
      · **调试中的版本**：在目标 PATCH 后加预发布号，如 `0.2.21-alpha.1`、`-alpha.2`。
        测试通过、正式确定之后，**去掉 `-alpha.N`、版本落成 `0.2.21`**。
        也就是说：修订号在测试通过前不落实，先在 alpha 上迭代。

    预发布段：不稳定版本，优先级**低于**同号正式版（0.2.21-alpha.1 < 0.2.21）。
      .NET 侧拆成两半，因为 FileVersion 只接受纯数字四段：
        VersionSuffix = alpha.N   ->  可读版本 "0.2.21-alpha.1"
        FileVersion   = X.Y.Z.N   ->  exe 文件属性里的数字版本（末段放预发布序号）
      没发预发布版时末段写 0（如 0.2.21.0）。
      比较优先级：alpha.2 < alpha.10（按数字比，不是字符串），alpha < alpha.1。

    ⚠️ 给 QA 出新包时**必须同时改这几处**（版本号 + FileVersion + Package.appxmanifest
    里的 Version），只改一处会让 exe 的文件属性仍显示旧版本，QA 无法从属性对话框分辨包的新旧。

    MSBuild 属性对应关系：
      VersionPrefix + VersionSuffix -> AssemblyInformationalVersion（程序读到的可读版本，如 0.2.21-alpha.1）
      FileVersion                   -> exe 文件属性「文件版本」（Windows 要求每段 0-65535）
      AssemblyVersion               -> 程序集绑定版本，只在 MAJOR 变动时改
  -->
  <PropertyGroup>
    <VersionPrefix>0.2.21</VersionPrefix>
    <!-- 预发布序号：调试中用 alpha.N；测试确认后留空并把 VersionPrefix 定为该版本 -->
    <VersionSuffix>alpha.1</VersionSuffix>

    <!-- FileVersion 只能是四段纯数字：MAJOR.MINOR.PATCH.<预发布序号>（无预发布时为 0） -->
    <FileVersion>0.2.21.1</FileVersion>
    <AssemblyVersion>0.2.0.0</AssemblyVersion>

    <Product>GameTimeTracker</Product>
    <Company>GameTimeTracker</Company>
    <Copyright>MIT License</Copyright>
  </PropertyGroup>

</Project>
