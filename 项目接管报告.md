# 项目接管报告

接管时间：2026-09-18　·　分析范围：`E:\vi2` 全量源码 + 运行时数据库（只读，未修改任何文件）

---

## 0. 先说一件必须先对齐的事：目录问题

你给的路径是 `E:\WorkBuddy\WBI\GameTimeTracker-v0.9.5-alpha17-win-x64`，并提到源码在 `test/`。

**实际情况是：**

- 这个工作目录里**没有一行源代码**。它是一个 .NET **发布产物**（`GameTimeTracker.App.exe` + `Core.dll` / `Infrastructure.dll` / `App.dll` + 大量运行时 DLL + 80 多个语言包 `.mui`）。里面的 `README.md` 是给用户看的下载说明。
- `test/` 目录**不存在**。
- **真正的源码在 `E:\vi2`** — 这是从 PDB 里的编译路径（`E:\vi2\src\GameTimeTracker.App\obj\x64\Release\...`）反查出来的，并已实际确认：包含 `src/`、`tests/`、`tracker/`、`dist/`、`logs/`、`config.json`、`CLEANUP-REPORT.md`。
- 另外，`E:\vi2` 里**同时躺着两代实现**：`src/` 是 C# / WinUI 3 **主线**，`tracker/` 是 Python **遗留线**（已停止演进）。

**下面的报告全部基于 `E:\vi2\src` 的真实源码。** 如果你希望我以工作目录那个发布产物为准，或者你有另一份 `test/` 的源码，请告诉我，我重新对齐。

> 附带发现：程序运行时不读工作目录。活跃数据库在 `%LocalAppData%\GameTimeTracker\gametime.db`。

---

## 1. 项目用途

### 解决什么问题

玩家想知道「我到底玩了多久游戏」，但手动记录很烦、Steam 只统计自家游戏、跨平台（Steam / Epic / Xbox / GOG / 育碧 / EA / WeGame / 手动添加）散落各处。这个程序**后台常驻自动计时**，并把结果**双向同步**到用户自己的 Notion 数据库，让 Notion 成为唯一的时长看板。

### 核心功能

1. **自动识别 + 计时** — 5 秒轮询进程，识别出游戏，自动开始/结束会话，跨午夜自动拆分成两天。
2. **Notion 双向同步** — 本地记录推到「每日时长表」；Notion 里的手动记录 / 总表条目 / 封面自动拉回。
3. **双向删除** — 任一侧删除，另一侧跟着对账（程序内删除有确认框；Notion 侧进回收站 30 天可恢复）。
4. **数据可视化** — 52 周年活动热力图、今日 Top 3、最近记录、连续天数、今日/本周环比。
5. **游戏匹配与绑定** — 本地检测到的游戏 ↔ Notion 总表条目，支持 Steam AppID 精确匹配、别名、模糊候选人工确认。

### 用户完整使用流程

1. 下载 zip 解压到任意目录（**不能放 Program Files**，程序需要写自己的目录）。
2. 双击 `GameTimeTracker.App.exe` — 无需安装任何运行时。
3. 在 Notion 建 Integration 拿 Token，把两个数据库 Connect 给该 Integration。
4. 在程序**设置页**填 Token + 两个数据库 ID（存进 SQLite `settings` 表）。
5. 之后全自动：托盘常驻 → 玩游戏自动计时 → 每 15 分钟自动同步 → 关窗口只是收进托盘。
6. 「待处理」页处理未绑定的新游戏；「映射库」页人工绑定 / 解绑。

---

## 2. 项目结构

```
E:\vi2\
├── GameTimeTracker.slnx          # 解决方案入口（四工程）
├── README.md                     # ⚠ 描述的是 Python 遗留线，已过时
├── CLEANUP-REPORT.md             # 上一轮的「最小必要优化」审计报告
├── HANDOVER.md                   # ← 本次新建（见第 12 节）
├── config.json / config.example.json   # ⚠ Python 线的配置，C# 线不读
├── .workbuddy/memory/            # 项目长期备忘（信息量很大，值得读）
│
├── src/
│   ├── GameTimeTracker.Core/            # 领域层：模型 + 接口 + 纯逻辑（零外部依赖）
│   │   ├── Models/DomainModels.cs       # GameRecord / GameSession / DailySummary / TrackerConfig ...
│   │   ├── Interfaces/Interfaces.cs     # IDatabaseRepository / INotionClient / INotionSyncService / IGameMatcher ...
│   │   └── Services/
│   │       ├── AppPaths.cs              # 数据目录解析（引导文件 + 环境变量）
│   │       ├── GameSessionManager.cs    # 会话生命周期 + 心跳 + 跨午夜拆分  ← 计时核心
│   │       ├── GameMatcher.cs           # 标题归一化 + 四级匹配 + Steam AppID 提取
│   │       └── DailyAggregator.cs       # 热力图 / 连续天数 / 环比 统计
│   │
│   ├── GameTimeTracker.Infrastructure/  # 实现层
│   │   ├── AppLog.cs                    # 轻量文件日志（2MB 轮转，线程安全）
│   │   ├── Database/SqliteRepository.cs # 全部 SQL（Dapper + WAL）  ← 1102 行，最重
│   │   ├── Notion/NotionServices.cs     # NotionClient + NotionSyncService  ← 1284 行，最复杂
│   │   ├── Process/Win32ProcessMonitor.cs  # 进程枚举 + 路径提取
│   │   ├── Process/ProcessFilter.cs     # 进程黑名单（浏览器/启动器/系统/工具/辅助）
│   │   ├── Platforms/*.cs               # 8 个平台检测器 + GameLibraryManager
│   │   └── Covers/CoverCacheService.cs  # 封面图标提取与缓存
│   │
│   └── GameTimeTracker.App/             # WinUI 3 表现层
│       ├── App.xaml(.cs)                # 入口，注册全局异常处理
│       ├── MainWindow.xaml(.cs)         # 编排核心：服务装配 + 两个后台循环 + 导航 + 托盘  ← 772 行
│       ├── ViewModels/ViewModels.cs     # HomeViewModel 等（656 行）
│       ├── Views/{Home,History,Pending,Mappings,Settings}Page   # 五个页面
│       ├── Dialogs/                     # 手动添加 / 游戏绑定 / 删除确认
│       ├── Controls/HeatmapControl      # 自绘热力图
│       ├── Converters/Converters.cs     # 本地图片内存流加载（Unpackaged WinUI 必需）
│       └── Tray/SystemTrayService.cs    # 托盘图标（动态生成）+ 右键菜单
│
├── tests/GameTimeTracker.Tests/         # xunit + FluentAssertions，44 个用例
├── tracker/                             # ⚠ Python 遗留线（models/detectors/notion/db/core）
├── Platforms/                           # ⚠ 与 src/.../Platforms 逐字节重复的死副本（已备份待删）
├── dist/                                # 发布产物 zip（v1.0.1 → v1.2.1，同一天发的）
└── logs/                                # Python 线的日志
```

---

## 3. 核心运行流程

### 入口

`App.OnLaunched` → `new MainWindow()`。**构造函数里完成了几乎所有装配**（服务实例化 → 托盘 → 首页 → 启动异步初始化链），业务逻辑从 `InitializeAndStartMonitoringAsync()` 开始。

### 采集链路（`MonitorLoopAsync`，每 5 秒）

```
扫描进程 (Win32ProcessMonitor，过滤黑名单)
  → 识别游戏 (GameLibraryManager + 8 平台检测器)
      └ 未识别 → 回退按 exe 路径/文件名查本地已注册游戏
  → GetOrCreateGameAsync (按 platform+platform_id，回退按 exe 路径)
  → 新会话 StartSessionAsync / 已有会话 HeartbeatSessionAsync
      └ 心跳：累计 delta 秒 → AddSessionDurationToDailyAsync
           └ 跨午夜 → 按 00:00 拆成昨天/今天两笔
  → 进程消失 → EndSessionAsync（同样处理跨午夜）
  → 清理僵尸会话 + 更新托盘状态 + 待处理角标
```

**关键设计**：时长不是「结束时一次性算」，而是**每次心跳增量累加**到 `daily_summary`。这样即使程序被强杀，已累加的时长不会丢。

### 同步链路（`PeriodicSyncLoopAsync`，每 15 分钟）

顺序是**刻意固定的，不可颠倒**：

```
1. RefreshGameCatalogCacheAsync   刷新总表快照（空结果不覆盖，防止清空）
2. ReconcileNotionDeletionsAsync  Notion 删了→本地跟着删（有两道安全阀）
3. AutoLinkGamesFromCatalogAsync  仅确定性信号自动绑定
4. PullDailyRecordsFromNotionAsync  把 Notion 手动记录认回本地（拿回 page_id）
5. BackfillRelationsAsync         补关系 + 写总表 page icon
6. SyncPendingDailyRecordsAsync   上传本地 pending 记录
```

**同步链有四个触发点**：启动、每 15 分钟、首页「立即同步」、**窗口重新获得焦点（60 秒节流）**。第四条是为了让「在 Notion 删完切回程序」立刻生效。

### 数据产生 / 处理 / 保存

| 阶段 | 位置 |
|---|---|
| 产生 | 进程扫描 → 会话心跳 |
| 处理 | `GameSessionManager` 累加与拆日 → `daily_summary` |
| 保存 | `%LocalAppData%\GameTimeTracker\gametime.db` |
| 展示 | `DailyAggregator` 聚合 → HomeViewModel → 五个页面 |
| 外发 | `NotionSyncService` → Notion 每日时长表 / 游戏总表 |

### 后台任务形态

- **两个 `Task.Run` 长循环**，共享一个 `CancellationTokenSource`。**没有** IPC、没有消息队列、没有 Windows 后台任务。
- 循环用 `Task.Delay(..., ct)`，`OperationCanceledException` 优雅退出。
- 两个循环的 `catch` 都是**静默**的（有意为之：后台线程不能崩）。

### 外部 API

只有 **Notion**（`https://api.notion.com/v1`，`Notion-Version: 2022-06-28`，20 秒超时，指数退避重试 3 次）+ **Steam 商店 API**（查中文名 / 封面，失败静默降级）。

### 配置与状态

- **配置**：SQLite `settings` 表（`notion_token` / `game_database_id` / `daily_database_id` / `theme_mode`）。启动时读入 `TrackerConfig` 内存对象。
- **状态**：会话状态在内存 `Dictionary + SemaphoreSlim`，同时持久化到 `sessions` 表。
- **数据位置**：`data_location.txt` 引导文件（自定义位置时）+ `GAMETIME_DB_PATH` 环境变量（最高优先级）。

---

## 4. 核心模块

| 模块 | 文件 | 职责 |
|---|---|---|
| **MainWindow** | `MainWindow.xaml.cs` | 服务装配、两个后台循环、页面缓存导航、托盘、主题 |
| **GameSessionManager** | `Core/Services` | 会话生命周期、心跳、跨午夜拆分、僵尸清理 |
| **SqliteRepository** | `Infrastructure/Database` | 全部 SQL、写锁串行化、WAL、幂等建表、加列兼容 |
| **NotionSyncService** | `Infrastructure/Notion` | 目录刷新、删除对账、自动关联、拉取、上传、双向删除 |
| **NotionClient** | 同上 | HTTP 封装、请求克隆重试、属性解析（标题/日期/时长/Relation） |
| **GameMatcher** | `Core/Services` | 标题归一化（全角/大小写/罗马数字/标点）、四级匹配 |
| **DailyAggregator** | `Core/Services` | 热力图、连续天数、环比、Top 游戏（纯函数，易测） |
| **ProcessFilter** | `Infrastructure/Process` | 进程黑名单（7 类 HashSet + 工具名正则 + 路径关键词） |
| **8 个平台检测器** | `Infrastructure/Platforms` | Steam(注册表+vdf+acf) / Epic / GOG / Ubisoft / EA / Xbox / WeGame / 手动 |
| **CoverCacheService** | `Infrastructure/Covers` | 图标提取（PrivateExtractIcons 256→48）、缓存、幂等 |
| **SystemTrayService** | `App/Tray` | 托盘图标（4 状态，GDI+ 动态绘制）、右键菜单、气泡通知 |

---

## 5. 当前已经完成的功能

- 进程扫描 + 8 平台游戏识别 + 手动添加游戏
- 会话计时 + 心跳增量累加 + **跨午夜自动拆分**
- 僵尸会话清理（启动 3 分钟阈值 + 每轮检查）
- 游戏库定期重扫（30 分钟节流，解决"运行期间新装游戏"）
- Notion 总表目录缓存 + 失效条目清理
- 四层游戏匹配（标识 / 精确 / 归一化 / 模糊候选）
- 自动关联（仅确定性信号）+ 手动绑定对话框
- 本地 → Notion 上传（创建 / 更新，标题 `游戏名 · X h`）
- Notion → 本地拉取（认出 page_id，防复活）
- 关系回填 + 总表 page icon 自动写入（仅 Steam、仅未设图标）
- **双向删除**：程序删（确认框 + 归档留底）/ Notion 删（对账 + 两道安全阀）
- `deleted_archive` 删除留底表
- 数据目录可迁移（引导文件 + 环境变量）
- 52 周年热力图 / 今日 Top3 / 最近记录 / 连续天数 / 环比
- 深浅色主题 + 标题栏按钮显式着色
- 系统托盘常驻（4 状态图标 + 菜单 + 通知）
- 关窗释放视觉树 + 工作集裁剪（252MB → 67MB）
- 全量文件日志（2MB 轮转）+ 全局未处理异常捕获
- 44 个单元测试（含防递归回归用例）

---

## 6. 已实现但存在风险/问题的功能

| # | 功能 | 问题 | 影响 | 状态 |
|---|---|---|---|---|
| 1 | 同步链 | `PullDailyRecordsFromNotionAsync` 被重复调用（`SyncPendingDailyRecordsAsync` 内部已含） | 多一次全量查询，浪费配额。**不产生错误数据** | 已记录 |
| 2 | 配置 | `SessionHeartbeatIntervalSeconds` / `AutoCreateGames` **未接线** | 改这两个值无任何效果，误导维护者 | 已记录 |
| 3 | 热力图 | 年度热力图用「窗口内相对最大值」，月度热力图**硬编码 360 分钟** | 同月两个视图颜色可能对不上 | user 已知 |
| 4 | 游戏匹配 | 库里存在**重复游戏行**：`Patch Quest`(manual) 与 `拼贴冒险传 Patch Quest`(steam 1347970) | 统计被拆两条；候选列表冗余 | 已记录 |
| 5 | 游戏匹配 | `Dogs Organized Neatly` 的 `platform_id = 76100bd5`（**非纯数字**，非合法 Steam AppID） | 该游戏无法通过标识匹配 | 待确认是否正常 |
| 6 | 目录缓存 | 残留假 page_id（`page_bg3` 等 5 条）；`ClearCatalogCacheAsync` 已实现但无人调用 | 候选列表可能含选不中的条目 | user 已知 |
| 7 | 后台循环 | 空 `catch{}` 静默吞异常 | 故障只能靠日志/归档表反推 | **有意设计，建议保留** |
| 8 | 封面 | 小图升级靠内存标记，重启才渐进生效 | 缓存里可能长期存在低分辨率图 | **设计取舍** |
| 9 | 删除对账 | `ReconcileNotionDeletionsAsync` 依赖 `game_catalog` 为"忠实快照"——若目录刷新恰好在总表权限异常时返回旧数据，对账基准会滞后 | 极端情况下漏删（偏保守，安全方向） | 需验证 |

> 说明：第 9 条是我在阅读时的**推理**，未在真机复现。标为**需要验证**。

---

## 7. 尚未完成或需要确认的功能

**需要你确认的**（我无法从代码判断设计意图）：

1. **`TrackerConfig.SessionHeartbeatIntervalSeconds` / `AutoCreateGames`** — 是「计划要接线」还是「废弃待删」？
2. **`Platforms/`（根目录 9 个 .cs）** — 已确认与 `src/.../Platforms/` 逐字节重复，是否同意删除？（备份已在 `.workbuddy/deadcode-backup/`）
3. **`tracker/` Python 遗留线** — 是否确认彻底冻结？是否从仓库移除以避免混淆？
4. **`README.md` 过时** — 它描述 Python 线且路径写 `e:\gemini\vi`，是否要改成 C# 线？
5. **`Dogs Organized Neatly` 的 `76100bd5`** — 这是历史遗留的随机 ID，还是有意为之？
6. **`config.json`** — Python 线的配置遗留在 C# 线仓库里，是否清理？
7. **`dist/` 里 1.0.1→1.2.1 共 15 个 zip** — 是否要清理历史版本？
8. **两个热力图阈值口径** — 是否要统一？统一到哪个口径？（会影响观感）

**尚未实现**（从代码看确实没有）：

- 自动更新 / 版本检查
- 数据导出 / 导入
- 多用户或云同步（当前完全依赖 Notion）
- 游戏时长目标 / 提醒 / 限制（`achievements_completed.svg` 这个资源存在，但代码里**没找到**成就系统的实现——**疑似预留或遗留资源，需要你确认**）
- 日志查看界面（用户要手动去 `data\logs\app.log`）

---

## 8. 我认为最值得关注的问题

按优先级排序（正确性 > 稳定性 > 兼容性）：

**① 删除链路是最高风险区，但当前防护是扎实的。**
双向删除涉及「本地物理删除 + Notion 归档 + 对账」。代码里有**两道安全阀**（远端空集合整段跳过、只处理 `synced` 行）+ **`deleted_archive` 留底** + **所有入口强制确认框**。
从真实库取证：`deleted_archive` 有 6 条记录（4 条 `notion`、1 条 `app`、1 条 `notion`），时间戳 09-17 18:51～19:13，**说明对账确实在跑**。
我的判断：**这块改动的容错成本极高，任何修改都必须先看清这两道安全阀**。

**② `NotionServices.cs` 1284 行是复杂度热点。**
一个类里同时承担：目录刷新、删除对账、自动关联、拉取、上传、双向删除、icon 写入。职责已经很重。
**但我目前不建议拆**——它内部有大量顺序耦合（尤其对账必须早于 Pull），拆开容易引入时序 bug。仅作标记。

**③ 静默 `catch{}` 是主要的可观测性缺口。**
后台循环、`OnMainWindowActivated`、多处初始化都是空 catch。出问题时**唯一线索是 `AppLog` 和数据库**。
考虑到这是「长时间后台运行」的程序，我建议（**待你确认后**）在关键 catch 里补 `AppLog.Warn`，但保持「不抛出不弹窗」的行为。

**④ 重复游戏行会污染统计。**
`Patch Quest` 两条记录是真实数据问题，不是代码 bug 的直接表现，但会让用户困惑。需要一次数据清理（**我不能擅自改你的数据**）。

**⑤ 项目没有版本控制。**
`E:\vi2` **不是 git 仓库**。在一份 10,000+ 行、还在活跃修改的项目上做维护，没有 VCS 意味着：
- 无法回滚
- 无法看历史 diff
- 每次改动都要手工备份

**这是我眼中最值得优先解决的基础设施问题。**（但需要你同意才能初始化）

---

## 9. 长时间运行稳定性

这是你特别关注的点。我逐项作答，并明确区分「代码证据」与「推断」。

### 内存管理

**做得好的地方**（有明确代码）：
- 关窗时完整释放视觉树（`ContentFrame.Content = null` + 5 个页面实例置 null + 清 `BitmapCache`）+ `GC.Collect` + `SetProcessWorkingSetSize(-1,-1)`。**实测 252MB → 67MB**。
- 热力图格子（52×7=364 个）随视觉树一起释放。
- 图片走内存流 + **静态保活列表**（正确做法；否则异步解码时流被 GC 会白屏）。

**潜在风险**：
- 静态保活列表（`StringToImageSourceConverter` 里）**只增不减**——这是我从代码推断的，**需要验证**：长时间运行、反复切换页面后该列表是否会无界增长。
- `_steamCnCache`、`_processCache` 等内存字典同理，未见淘汰策略。**需要验证**。
- 窗口**不隐藏**（一直开着）时，`ReleaseVisualTree` 不会触发，视觉树常驻内存。

**结论**：托盘常驻场景（用户主用法）内存管理良好。**长期开着窗口**的场景未验证。

### 线程 / 异步任务

- 两个 `Task.Run` 长循环 + `SemaphoreSlim` 保护会话字典。**没有 async void 泄漏到后台**（`async void` 仅用于事件处理器，是 WinUI 惯例）。
- `MainWindow` 构造里 `_ = InitializeAndStartMonitoringAsync()` 是 fire-and-forget，异常会被静默吞掉（**风险**：启动链失败无人知晓）。
- **数据竞争**：`GameSessionManager._activeSessions` 被 `SemaphoreSlim` 保护，但 `GetActiveSessions()` 用的是独立的 `lock`，**两种同步机制混用在同一数据结构上** —— 这是我从代码识别出的**潜在不一致点**，标为**需要验证**（实际触发需高并发，命中概率可能很低）。
- `SqliteRepository` 的写操作统一走 `_writeLock` 串行化，读操作不加锁（WAL 支持读写并发）。设计合理。

### 文件 / 数据库

- SQLite 开 WAL + `synchronous=NORMAL` + `foreign_keys=ON`。**每个操作新建连接**（连接池由 ADO.NET 管理）。
- **写锁是全局的**：所有写操作串行。单用户场景下开销可忽略。
- **WAL 文件不会自动收缩**：当前 `gametime.db-wal` 358KB。长时间高频写入后 WAL 可能变大（SQLite 会在 checkpoint 时收缩，但**长期运行的实际表现需要验证**）。
- 日志：2MB 轮转、只留一代，**不会无限增长**。设计正确。
- 加列用 `try { ALTER } catch {}` —— 幂等，但**吞掉真实错误**（例如磁盘满时也会静默）。

### 网络 / API

- Notion 请求：20 秒超时、最多 3 次重试、指数退避（×1.5）、**正确克隆 HttpRequestMessage**（这是个经典陷阱，作者处理对了）。
- **429 尊重 `Retry-After`**；5xx 重试；4xx 抛出并**翻译成可行动的中文文案**（401=Token 无效、403=数据库没 Connect）。
- **网络断开行为**：`_lastPullSucceeded = false` → **本轮跳过上传**（宁可空跑，也不冒重复行风险）。这是本轮刚加的改动，**设计正确**。
- 推送失败 → 标记 `sync_status='error'` + 记 `error_message`，下一轮重试。
- **已删除页面**（`archived`）→ 本地跟随清理，避免永久卡在 error。设计正确。
- 隐患：`error` 状态的记录会**每轮无限重试**，没有次数上限或退避升级。**需要验证**长期表现。

### 崩溃恢复

- 全局兜底：`Application.UnhandledException` + `AppDomain.UnhandledException` → 写日志。
- **但栈溢出（`0xC00000FD`）捕获不了** —— 历史上正是因为封面递归导致栈溢出闪退，全局处理器完全没记录，最后靠 **stderr 重定向**才拿到托管栈。
- 另外 `0xC000027B`（WinRT 层异常）也捕获不到。
- **已修复的崩溃**：封面事件无限递归（栈溢出）→ 已改幂等 + 防重入，有回归测试。
- **崩溃转储位置**：`%LocalAppData%\CrashDumps\`（历史 6 个 `.dmp`，17:30 后未再出现）。

### 重启恢复

- **会话恢复**：启动调 `CleanupStaleSessionsAsync(3min)` 关闭过期会话（`end_time = last_heartbeat`），再 `GetActiveSessionsAsync` 载入仍然活跃的会话继续计时。✅
- **时长不丢**：因为时长是心跳**增量累加**，强杀最多丢 5 秒。✅
- **僵尸会话**：`CleanupZombieSessionsAsync` 每轮检查 PID 是否还存在。✅
- **同步恢复**：`pending` / `error` 记录下次同步自动重试。✅
- **状态一致性**：`daily_summary` 用 `(date, game_id)` 唯一约束 + `MAX` 合并，**重复拉取是幂等的**。✅
- **注意**：`CleanupStaleSessionsAsync` 把 `end_time = last_heartbeat` 但不重算 `daily_summary` —— 意味着崩溃前最后一段（最多 3 分钟阈值 + 5 秒）的时长**可能未累加**。这是**有意的时间精度取舍**，标为**需要确认**是否符合预期。

### 连续运行数天/数周的判断

**能够正常工作的方面**（有代码证据）：内存（托盘场景）、日志轮转、数据库幂等、崩溃恢复、网络重试、时长累加。

**我持保留意见、需要实测的方面**：
1. 静态图片保活列表 / 内存字典的长期增长（最可疑）
2. WAL 文件长期膨胀
3. `error` 状态记录的无限重试
4. `_activeSessions` 上两种同步机制混用
5. 长时间不关窗口时的视觉树常驻

**结论**：架构上**具备**长期运行的基础（这是被认真设计过的），但**没有做过真正的长时间压测证据**。建议做一次 72 小时连续运行观测（这是我这轮**没有做**的事，需要你授权才能真正跑）。

---

## 10. 目前不建议修改的部分

**强烈不建议动**：

1. **`SqliteRepository` 的 Dapper 下划线映射开关** — 关掉会静默丢字段。
2. **DDL 与表结构** — 已发布多个版本，改结构有数据迁移风险。
3. **删除链路的两道安全阀 + `deleted_archive` 归档** — 容错成本极高。
4. **同步链的六步顺序** — 顺序耦合是刻意的。
5. **`DurationSuffixRegex` 的旧格式兼容** — Notion 里有历史行。
6. **`GameSessionManager` 的拆分逻辑** — 跨午夜是最容易改出 bug 的地方。
7. **封面幂等 + `HasCoverFile()`** — 这是修复栈溢出闪退的关键，别改回 `Length > 1500`。
8. **所有删除入口的确认框** — 曾经因为没有确认框误删了 6 款游戏。
9. **`Converters` 的内存流 + 静态保活** — Unpackaged WinUI 3 唯一可行方案。
10. **后台循环的「不回抛」行为** — 静态吞异常是有意的。
11. **UI 细节约定** — 见 `HANDOVER.md` 第 2.5 节（封面无底色、Hero 背景只认总表 cover、两种排序口径等），都是用户明确拍板过的。

**不建议主动重构**：

- `NotionServices.cs`（1284 行）：职责过重，但内部顺序耦合强，拆分风险大于收益。**标记为「想拆请先跟我讨论」**。
- 三工程分层结构：清晰合理，无需调整。

---

## 11. 需要我确认的问题

**关于项目认知**：
1. 源码在 `E:\vi2`（不是工作目录的发布产物，也没有 `test/`）——**这个理解对吗？**
2. `src/` 是唯一主线、`tracker/` Python 线已冻结——**确认吗？**
3. 工作目录 `GameTimeTracker-v0.9.5-alpha17-win-x64` 与 `dist/` 里 `v1.2.1` 的对应关系是什么？版本号命名不一致（v0.9.5-alpha17 vs v1.2.1），**哪个是当前发布版？**

**关于待确认的功能**（详见第 7 节）：4 个配置项/遗留物的处置、`achievements_completed.svg` 的用途、重复游戏行的清理。

**关于维护方式**：
- 要不要初始化 git？（**我认为这是当前最值得做的一件事**）
- 是否需要我做 72 小时长时间运行观测？
- `README.md` 是否要修正为 C# 线？

---

## 12. HANDOVER.md

**已创建：`E:\vi2\HANDOVER.md`**

你说得对——「交接提示词」不应该承担全部上下文。我把**人为确定的设计决策**沉淀成了这份文件，包含：

- **0. 一分钟速览** — 快速建立坐标
- **1. 当前状态** — 已完成 / 正在处理 / 未处理三段
- **2. 已确定的设计决策** — 技术选型、数据库、Notion、游戏匹配、UI 约定、数据位置（**核心章节**）
- **3. 已知问题** — 6 个具体问题及其影响与状态
- **4. 不要做的事情**
- **5. 提交与验证纪律** — 含**9 个代价很大的坑**（NOT NULL、Dapper 映射、封面递归、栈溢出取证方法等）
- **6. 维护节奏建议**

这样新 Agent 的路径就是：

```
新 Agent → 读 HANDOVER.md → 读 src/ 全部代码
        → 输出「项目接管报告」→ 你确认理解正确 → 开始逐项维护
```

---

## 补充：按你的要求，我做了什么 / 没做什么

**做了（只读）**：
- 递归检查 `E:\WorkBuddy\WBI\GameTimeTracker-v0.9.5-alpha17-win-x64`（确认是发布产物）
- 反查 PDB 编译路径，定位真实源码 `E:\vi2`
- 通读 `src/` 全部源码（约 10,400 行）+ `tests/` + 配置文件 + `.workbuddy/memory/`
- 提取 `.dll` 元数据交叉验证类型结构
- **只读**打开活跃数据库副本（`tempfile` 拷贝，未触碰原库）核对表结构、数据量、`deleted_archive`、游戏绑定状态
- **新建 `E:\vi2\HANDOVER.md`**（唯一写操作）

**明确没做**：
- ❌ 未修改任何源码
- ❌ 未修改数据库
- ❌ 未删除任何文件
- ❌ 未重构
- ❌ 未能构建/测试（`dotnet` 不在当前 PATH 中，无法验证 44 个用例是否仍然全过 —— **需要你确认环境或告诉我 SDK 路径**）

**报告到此为止，等你提出下一步需求。**
