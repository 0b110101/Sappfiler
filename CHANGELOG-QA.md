# GameTimeTracker 版本变更记录（给 QA）

> 每交一版调试包，在**最上面**加一节，写明「改了什么 / 要重点验证什么」。
> 版本号规则见 `HANDOVER.md` 2.7。
>
> **验证状态图例**：✅ 已构建验证 ／ ⚠️ 待验证（代码已写但未经构建）

---

## v0.3.1-alpha.2 — 2026-09-24

**验证状态**：✅ **代码已加固并通过全量单元测试与打包构建**
**核心改动与修复**：
1. **品牌形象升级为 Sappfiler**：
   - 应用名从 GameTimeTracker 正式更名为 **Sappfiler**（包括窗口标题、托盘信息、应用清单包信息）。
   - 全套桌面与应用图标（AppIcon.ico、logo.png、所有清单磁贴与启动页 PNG）全面替换为官方新视觉 Logo。
2. **免侵权字体嵌入与等宽排版优化**：
   - 将开源免费商用字体 `Geist`（英数）与 `OPPO Sans 4.0`（中文字体）全量嵌入安装包（`Assets/Fonts/`），并配置全局 XAML 字体回落链路。
   - 所有统计数字、百分比与图表刻度开启 Tabular 等宽对齐（`Typography.NumeralAlignment="Tabular"`），杜绝刷新跳动。
3. **【游戏探索】增减变化指示（Delta Badge）**：
   - 探索卡片右侧头像堆叠后不再显示 +N 溢出徽章，而是显示对比上一周期的真实增减差值（如 `+2`、`-1`，无变化时优雅隐藏）。
   - 全周期跨度（月/季/年）均支持向前回溯计算上期的分类基准。
4. **进度条色彩阶梯规范统一**：
   - 遵照设计要求，所有排行与活跃度进度条严格统一采用「最少至最多」六级色阶阶梯：最少 (灰 `#94A3B8`) → (绿 `#10B981`) → (蓝 `#3B82F6`) → (黄 `#F59E0B`) → (紫 `#8B5CF6`) → (橙 `#F97316`) 最多。
   - 环形图保留多维分类专属配色，不受单调色阶干扰。
5. **动态周期切换全域联动**：
   - 统计页所有卡片（探索情况、游玩趋势折线图、时长排行、时长分布、游戏活跃度）全面深度联动顶部时间控件（月/季/年与前后翻页），自测保证无死角响应。
6. **QA 历史超长天数与多端同步鲁棒性修复**：
   - 修复游戏连续游玩天数（Streak）受单一表跨度截断的问题，基于全量记录真实追溯。
   - 游戏多实例进程并发防重与去重判定，彻底消除同一款游戏时长虚增与多进程记录偏差问题。

---

## v0.3.1-alpha.1 — 2026-09-20

**验证状态**：✅ **代码已加固并通过全量 161 项单元测试与 0 警告构建**
**核心改动与修复**：
1. **单实例进程保护（防重复运行）**：
   - 引入全局互斥体（Named Mutex）与 Win32 广播激活机制（`RegisterWindowMessage` + `AllowSetForegroundWindow`）。
   - 若用户误点重复启动，新进程立即退出，并自动将已在后台或托盘中的主窗口还原、呼出并置于最前台，彻底杜绝多开冲突与资源浪费。
2. **开源协议与非商业条款确立**：
   - 确立采用 **CC BY-NC-SA 4.0** 许可协议，明确禁止任何商业化倒卖、付费分发、广告植入或商用集成。
   - 同步更新 `LICENSE`、`README.md` 及全工程 PropertyGroup 描述。
3. **【统计页】排行与记录展示调优**：
   - 「游戏时长排行」扩展为 Top 6 并保持金银铜荣誉奖章与主题自适应；「本月游戏记录」彻底去除序号徽章，界面更清爽。
4. **【统计页】游戏时长分布环形图重构**：
   - 环形图仅展示当前周期时长 Top 5 的游戏，去除「其他游戏」分片，百分比统一为该游戏时长占全局总时长的绝对比例。
   - 增加底层灰色轨道（Background Track），在不足 100% 时维持视觉完整度。
5. **【统计页】趋势折线图粒度与 Tooltip 规范**：
   - **自然季度模式**：粒度固定为每周（周日截止），数据点约 13~14 个，隔周标注 X 轴日期；Tooltip 规范展示格式（周区间、总游戏时长、游戏款数）。
   - **自然年模式**：粒度固定为每月，严格 12 个数据点，彻底消除「心电图」高频折线。
6. **右上角周期导航器与 Tabler 矢量图标统一**：
   - 周期标题根据当前模式动态呈现：月度（`2026年9月`）、季度（`2026 Q3`）、年度（`2026年`），左右步进分别对应 1 个月、3 个月、1 年。
   - 切换箭头统一采用项目标准的 Tabler 矢量图标。
7. **QA 闪退根因修复（`LayoutCycleException`）**：
   - 尺寸变化增加 `< 1.0px` 阈值防抖，杜绝 WinUI 测量亚像素死循环。
   - 数据刷新引入 `SemaphoreSlim` 信号量互斥排队锁，避免快速连点周期导致集合多线程冲突。
8. **开机自启动与后台纯粹低占用模式**：
   - 增加开机自启动设置项（默认关闭，注册表安全写入）。
   - 最小化至托盘时安全裁剪工作集与缓存，待机内存降至 ~25MB 级。

---

## v0.2.20-alpha.7 — 2026-09-19

**验证状态**：✅ **代码已加固并通过全量 144 项单元测试与 0 警告 Release 编译**
**核心改动与修复**：
1. **多游戏轮播初始化、切换速率与读秒速度异常根治（彻底解决"刚开始两次同游戏才切换、读秒异常、运行越久越正常"）**：
   - **根本原因排查分析**：
     - **原因一（列表排序动态翻转）**：原逻辑在 `distinctGames` 中按 `OrderByDescending(g => g.EarliestStartTime)` 动态排序。在应用启动或多进程启动初期，多款游戏的启动时间戳几乎相同或发生毫秒级微小更新，甚至启动器进程（Launcher）在几秒后退出时，`EarliestStartTime` 会发生改变，导致 LINQ 排序结果在两次计时器 tick 之间**动态翻转位置**！如果第 1 秒 Game A 在 index 0，第 5 秒准备切到 index 1 时列表翻转使得 Game A 变成了 index 1，就会导致卡片播放切换动效后**连续两次显示同一款游戏**！
     - **原因二（`RefreshAllDataAsync` 暴力覆盖当前展示游戏）**：新会话启动或后台刷新时会调用 `RefreshAllDataAsync()`，其末尾调用了 `RefreshHeroCardAsync(null)`，内部写死取最新启动的会话，在轮播器毫不知情的情况下突兀将卡片强行扭回 Game A，破坏了正在进行的轮播，导致下一秒轮播触发时再次切到 Game A！
     - **原因三（新游戏启动时轮播计数未重置）**：当第二款游戏启动时，`_heroCarouselTick` 处于中间计数值（例如已累加到 3 或 4 秒），导致新游戏刚在卡片出现 1~2 秒就被突兀切走，表现为"刚开始切换速率不对"；
     - **原因四（过渡动效期间秒数未保护）**：在执行 330ms 流光切换动画时，读秒计时与心跳未加锁，导致在新旧游戏状态交替的瞬间秒数突变；而在运行很久后，启动器全部退出、所有游戏会话稳定，上述竞争消失，所以"时间运行越久越正常"。
   - **加固解决方案**：
     - **确定性稳定排序**：封装 `GetDistinctActiveGames()`，严格采用 `OrderBy(g => g.GameId)` 升序排列，排序结果与时间戳彻底解耦，在游戏运行期间 100% 绝对稳定不可变；
     - **保护正在展示的卡片游戏**：`RefreshHeroCardAsync(null)` 改造为优先检查当前展示的 `_activeGameId` 是否仍在运行列表中，如果仍在运行则**坚决保持**，绝不突兀打断轮播；
     - **会话启动精准对齐**：在 `OnSessionStarted` 中找到新启动游戏在列表中的位置，精准同步 `_heroCarouselIndex` 并将 `_heroCarouselTick` 归零，保证新启动的游戏完整展示满完整的 5 秒，绝不在 1~2 秒内被切走；
     - **动效状态互斥锁（`_isHeroTransitioning`）**：在退场微缩渐隐（150ms）至进场微放渐显（180ms）的动画全周期内加锁，数据与秒数在卡片完全透明的时刻同步切换，UI 线程直通执行，读秒从第 1 秒起均匀单调递增，无任何跳秒或停顿。
     - **新增自动化测试**：`MultiSessionTests.MultiProcessGame_GroupsByGameId_AndMaintainsDeterministicOrder` 全量通过。
2. **界面读秒抽搐彻底消除：UI 视觉跑表与后台 5 秒心跳数据层彻底解耦**：
   - **抽搐根本原因**：后台进程扫描循环每 5 秒触发心跳 `HeartbeatSessionAsync`，原先通过 `OnSessionHeartbeat` 异步穿透到 UI 线程强行覆写界面的 `_currentElapsedSeconds`。由于后台 5 秒心跳的到达时刻（如 4.8 秒）与 UI 线程每秒定时器（如 5.0 秒）存在**天然相位差**，心跳刚把秒数提前 0.2 秒拔到 5，200ms 后 UI 定时器又执行了一次 +1 把它变成 6，导致肉眼可见的"0.2 秒内连续跳 2 秒"（抽搐/双跳）；且多进程游戏每 5 秒会发送多次心跳，更加剧了秒表抽搐；
   - **最佳实践架构解耦**：
     - **数据层（数据库落库 + Notion 同步）**：完全由 `GameSessionManager` 的后台 5 秒心跳与进程退出事件驱动，精准聚合，落库与同步逻辑 100% 独立可靠；
     - **表现层（界面 Hero 跑表）**：切断 `OnSessionHeartbeat` 对 UI 秒数变量的侵入。界面进入游戏或轮播切入时对齐基准时间，随后由 UI 线程的 `DispatcherTimer` 独立平稳跑秒（精确每 1000ms 递增 1 秒，平稳丝滑）；
     - **大偏差静默对齐（容灾）**：仅在电脑睡眠唤醒等极端情况下导致物理墙钟与界面秒数偏差超过 3 秒时，才执行静默平滑对齐；平时正常游玩时读秒绝对均匀稳定，0 抽搐，0 抖动。
3. **左侧菜单栏、快捷按钮与「最近记录」全套升级为统一风格的 Tabler 矢量线框图标**：
   - **全面重构升级**：彻底移除老式 Windows 原生字体图标（`FontIcon`），统一升级为现代 App 高级线框矢量路径（`<Viewbox><Canvas><Path .../></Canvas></Viewbox>`）：
     - **今日（Home）**：`tabler:home` 房屋线框；
     - **历史（History）**：`tabler:history` 逆时针箭头时钟线框；
     - **待处理（Pending）**：`tabler:alert-triangle` 三角警告感叹号线框；
     - **游戏映射（Mappings）**：`tabler:link` 链条关联映射线框；
     - **设置（Settings）**：`tabler:settings` 机械齿轮线框；
     - **添加游戏按钮**：`tabler:plus` 精致加号线框；
     - **打开 Notion 按钮**：`tabler:external-link` 右上角外链箭头线框；
     - **「最近记录」标题前**：由字体图标升级为 `tabler:clock`（22×22 精致时钟线框），与今日游戏、同步状态、热力图、里程碑四大卡片标题前图标在**物理渲染尺寸（22×22）、线宽（2px）与颜色（`TextPrimaryBrush`）上严格 1:1 绝对平齐对齐**！
   - **动态高亮着色联动**：在 `SetActiveNav` 中将 `Path.Stroke` 直接接入选中高亮画刷（`AccentBlueBrush`）与默认文字画刷（`TextSecondaryBrush`），切换流畅自然。
1. **跑马灯文本下沉与鼠标悬停抽搐彻底根治（`MarqueeTextBlock`）**：
   - **根本原因**：此前当鼠标移入时执行了 `NormalTextBlock.Visibility = Collapsed`。由于容器内部的高度是由文本撑起的，一旦它变为 `Collapsed`，外层控件瞬间坍塌为 0 高度，导致光标瞬间失去悬停（触发 `PointerExited`）又立刻恢复（触发 `PointerEntered`），形成 60Hz 的剧烈抽搐循环；且 `Canvas.SetTop` 计算未考虑渲染基线，导致切换时文字垂直向下掉落；
   - **加固方案**：
     - `NormalTextBlock` 始终保持 `Visibility = Visible` 充当永久物理布局骨架（绝不坍塌、绝无重排），仅通过 `Opacity = 0 / 1` 切换显示；
     - 悬停启动跑马灯时，通过 `NormalTextBlock.TransformToVisual(RootContainer)` 精准获取当前文本块在容器内的**亚像素级物理渲染绝对坐标**并赋值给 `Canvas.SetLeft` 和 `Canvas.SetTop`，保证启动瞬间完全 1:1 重合，**绝不下沉、绝无抖动、顺滑自然**。
2. **正在游玩卡片鼠标悬停自动暂停轮播**：
   - 当鼠标移入正在游玩卡片（含游戏名跑马灯、时长、图标等任何区域）时，自动触发 `IsHeroCarouselPaused = true`，轮播倒计时立即暂停；移出后自然恢复，方便用户静心查看游戏名称或点击商店链接。
3. **轮播过渡动画全面升级为现代 App「流光切换」（缩放 + 淡入淡出）**：
   - 彻底废弃老式 PPT 左右平移滑动，采用现代化居中流光呼吸质感：
     - **退场（150ms）**：旧游戏卡片以中心为轴微缩（Scale: $1.0 \to 0.96$）并渐隐（Opacity: $1.0 \to 0.0$）；
     - **切源**：主线程瞬间切换数据模型；
     - **进场（180ms）**：新游戏卡片从 $0.98$ 微放至 $1.0$ 并渐显（Opacity: $0.0 \to 1.0$）；
     - 总动画时长 330ms，紧凑克制、质感拉满。
4. **跨日自动刷新机制加固（解决 24 点整界面未自动刷新的问题）**：
   - **根本原因**：之前「今日游戏」、「连续游玩里程碑」等数据计算均依赖 `RefreshAllDataAsync()`，而该方法仅在「应用启动、导航切回主页、游戏启停、修改设置」时触发。若软件保持打开停在主页面，在 23:59:59 跨过 24 点（或用户配置的跨日结算点）时，UI 缺少跨日监听定时器，导致界面一直停留在昨天的统计视图中。
   - **加固方案**：在 `HomeViewModel` 中引入轻量级跨日检测定时器（`_dayBoundaryTimer`），以极低开销检测当前时间戳与当前加载日期的差异。一旦跨过 24 点（或自定义跨日结算时刻），无需用户任何操作，界面秒级自动触发无感重算刷新，今日游戏自动清零、今日总计与里程碑无缝切换至新的一天。
2. **多游戏轮播过渡效果锁死排查与丝滑滑动修复（Slide & Crossfade）**：
   - **根本原因**：`MainWindow.xaml.cs` 中采用 `ContentFrame.Content = _homePage;` 直接赋值挂载，未触发 `Frame.Navigate()` 导致 `HomePage.xaml.cs` 的 `OnNavigatedTo` 根本不会执行，动画代理 `ViewModel.HeroTransitionHandler` 一直为 `null`；且 WinUI 3 中 `DoubleAnimation` 未显式配置 `EnableDependentAnimation = true` 导致动画被渲染管线忽略。
   - **修复**：改在 `HomePage` 的 `ViewModel` setter 和 `Loaded` 生命周期内直接绑定；设计了向左平滑滑出淡出（180ms，0 -> -20px，1.0 -> 0.0）+ 从右平滑滑入淡入（240ms，+20px -> 0，0.0 -> 1.0）的双向硬件加速过渡效果。
2. **彻底解决读秒差距大、抽搐校准时间的问题**：
   - **根本原因**：许多大型游戏存在多进程（启动器 Launcher + 主程序 Game、崩溃收集器等），它们虽然属于同一款游戏（相同 `GameId`），但在本地被记录为不同 PID 的多个子会话，且启动时间和已记录时长差异极大（例如启动器运行了 30 分钟，主程序刚启动 2 分钟）。原先的 5 秒轮播逻辑仅判断 `sessions.Count > 1`，导致在同一款游戏的多个子进程之间反复横跳切换，加上后台心跳异步覆盖 UI 读秒，造成界面读秒大幅来回抽搐！
   - **加固方案**：
     - 在 UI 与轮播层严格按 `GameId` 分组去重，只有真正运行了**多款不同游戏**时才会触发轮播切换，彻底杜绝单游戏多进程的伪轮播；
     - 汇总该游戏所有子进程会话：取最早启动时间与最大时长；
     - 维护单调递增秒表 `_currentElapsedSeconds`，每秒递增，心跳仅向上对齐校准，**严禁读秒向后倒退或跳动**。
2. **进度条样式重构与调细 1/3（`RoundedProgressBar`）**：
   - **根因确认**：WinUI 3 原生 `ProgressBar` 内部指示器轨道的实际渲染高度被平台模板硬编码的 `ProgressBarTrackHeight` 锁死（默认仅 3~4px 细线）。在外部设置 `Height="20"` 只能撑大外层网格边界，不仅无法让进度条变粗，反而造成整行出现大面积空白虚高；
   - **重构方案**：封装专用轻量自定义控件 `RoundedProgressBar`，由双层圆角边框驱动，进度条渲染高度 100% 与 `Height` 属性严格 1:1 等高，支持 `CubicEase` 平滑加载动画与渐变色；
   - **尺寸按要求调细 1/3**：
     - **今日游戏单项**：由 10px 调细至 **7px** 胶囊圆角（CornerRadius 3.5px），封面缩略图调谐至 **48×48**（圆角 8px），与右侧「游戏名 + 时长/占比 + 真实进度条」总高度严格平齐；
     - **游玩里程碑**：由 12px 调细至 **8px** 胶囊圆角（CornerRadius 4px），粗细适中，上下间距舒适。
3. **彻底修复跑马灯未显示完整名字即被截断的 Bug**：
   - **根本原因**：在原有控件中，测量直接在带有 `TextTrimming="CharacterEllipsis"` 的已挂载节点上执行。在 WinUI 3 布局系统中，直接对已在树中的节点调用 `Measure(Infinity)` 会受父级容器布局缓存约束，返回的是带省略号的宽度加少量冗余，导致计算出的滚动距离仅够滚出 1~3 个字就被裁切了；
   - **架构重构**：采用双层解耦设计——静止时由 `NormalTextBlock` 原生高性能处理字符省略；悬停时在零宽度约束的 `Canvas` 容器中动态唤醒 `MarqueeTextBlockElement`（`TextTrimming="None"`），精确测量全局所有文字总像素，确保滚动距离覆盖到最后一个字并预留 24px 留白缓冲，且容器边界严格由 `RectangleGeometry` 剪裁，绝不越界遮挡其它列。
4. **卡片标题图标颜色与「同步状态」全要素颜色动态联动**：
   - **最近记录**：标题前时钟图标颜色统一修改为与标题文字相同的颜色（`TextPrimaryBrush`）；
   - **同步状态（云图标 + 标题文字「同步状态」+ 状态指示圆点）全联动**：
     - **成功**：**绿色**（`StatusGreenBrush`）—— 云图标、标题「同步状态」、状态圆点均变绿；
     - **待处理 / 待同步**：**黄色**（`StatusOrangeBrush`）—— 云图标、标题「同步状态」、状态圆点均变黄；
     - **本地模式**：**黑色 / 标题色**（`TextPrimaryBrush`，与其他卡片小标题颜色完全一致）；
     - **同步异常 / 失败**：**红色**（`StatusRedBrush`）—— 云图标、标题「同步状态」、状态圆点均变红；
     - 待同步与待处理均统一采用指定的 `cloud-check` 矢量云图标。
5. **托盘右键菜单「待处理」计数剔除已忽略游戏**：
   - **根因**：托盘右键菜单 `ShowContextMenu()` 中统计待处理时直接取了 `pending.Count`，误将标记为 `ignored`（已忽略）的游戏也计算了进去，与主窗口侧边栏角标（已剔除忽略项）产生数值不一致；
   - **修复**：增加 `p.Status != "ignored"` 过滤，确保托盘菜单、侧边栏角标、待处理页面三者显示完全一致（新增自动化单元测试覆盖）。
4. **彻底根除 Notion 对账误删本地记录 Bug（9.19 法老马赛克事件修复）**：
   - **根本原因**：当游戏退出后自动推送到 Notion 时，Notion 接口成功创建了每日记录页面（`POST /pages`）。随后用户切换窗口触发了删除对账 `ReconcileNotionDeletionsAsync`，该方法调用 Notion 数据库全量查询接口（`POST /databases/{id}/query`）。然而，Notion 的全局数据库查询与检索索引存在明显的**最终一致性（Eventual Consistency）延迟**（通常滞后数秒至十几秒甚至数十秒），导致刚刚推上去的新页面在数据库查询结果中暂未列出。之前的对账逻辑简单粗暴地认为"全量列表没有 = Notion 删除了"，直接执行物理删除，将本地刚记录的今日时长误清空！
   - **加固方案**：
     - 引入主库直接验证接口 `IsPageDeletedAsync`（调用 `GET /pages/{cleanPageId}`），不依赖有延迟的检索索引；
     - 在删除对账发现本地有、但列表查询未包含的候选记录时，**逐一向 Notion 进行二次确权**：只有当 Notion 明确返回 HTTP 404 或 `archived=true` / `in_trash=true`（用户确实在 Notion 删除了）时，才允许从本地清理；
     - 如果页面依然存在且未归档，坚决保留本地记录并记日志拦截，彻底阻断由于远端索引延时造成的误删！
4. **SemVer 规范纠正**：
   - 调试测试阶段不直接递增修订号（PATCH），统一保持 `0.2.20-alpha.7`，待正式验证后再确定是否升级版本；
   - 同步修正 `Directory.Build.props` 与 `Package.appxmanifest` 为 `0.2.20.7`。
2. **统计卡片重构（今日总计 / 本周总计）**：
   - 彻底对齐设计图（图 1）：
     - 左侧：统一采用 44×44 圆形浅蓝背景徽章 + 日历图标；
     - 中间：垂直排列「今日总计/本周总计」与大字号清晰时长（格式如 `6h 20m`）；
     - 右侧：移除绿色药丸胶囊底色，垂直靠右对齐展示百分比变动（如 `↑ 12%`）及对比文案（`较昨日` / `较上周`）。
3. **今日游戏卡片精修与右侧仪表板重排**：
   - **卡片位置对调**：右侧列将「同步状态」调整至顶部第一位，「今日游戏」调整至第二位；
   - **封面尺寸重调**：海报图标由小矩形扩充为 52×52 正方形，高度与「游戏名 + 时长 + 进度条」总高度严格平齐；
   - **展开/收起交互机制**：
     - 右上角按钮由「查看全部 >」改为「查看全部 ∨」（展开后为「收起 ∧」）；
     - 点击后就地展开今日游玩的所有游戏，不再跳转至映射菜单；
     - 当今日游戏不超过 3 个时，自动隐藏该按钮；超过 3 个时才展示展开按钮。
4. **游玩里程碑成就段位 Emoji 补齐**：
   - 补齐进度条下方当前段位称号缺少的前置 Emoji（`🎮 Lv.1`, `🌿 Lv.2`, `⭐ Lv.3`, `💎 Lv.4`, `🏆 Lv.5`），与成就悬浮窗完全对齐。
5. **深色模式热力图配色与边框精修**：
   - 深色模式下彻底去除所有格子的描边效果（边框透明度设为 0）；
   - 单元格配色严格对齐指定色盘：
     - 空白格（Level 0）：`#2A2D45`
     - Level 1：`#3C3E5F`
     - Level 2：`#606187`
     - Level 3：`#8485AF`
     - Level 4：`#A8A8D7`
     - Level 5：`#CCCCFF`
6. **正在游玩 Hero 卡片背景清晰度调优**：
   - Notion 游戏海报背景画刷不透明度由 0.18 提升至 0.30；
   - 压暗遮罩由 0.38 降低至 0.18，背景图显现更加清晰自然，同时保证文字高可读性。
7. **全新矢量图标库接入与品牌色适配（根据 icon.txt）**：
   - 替换占位图与功能图标为精细矢量 Path：游戏封面缺省占位图与今日游戏卡片采用精致游戏手柄图标、统计卡片采用日历网格图标、同步状态采用云朵对勾图标、连续游玩里程碑采用冠军奖杯图标；
   - 原生支持明暗主题色动态切换（`{ThemeResource AccentBlueBrush}`、`StatusGreenBrush`、`AccentPurpleBrush`、`TextSecondaryBrush`），并在 `Assets/icons` 保存对应的 SVG 矢量底稿。
8. **进度条加粗一倍与今日游戏排版严格还原**：
   - **进度条加粗**：今日游戏进度条高度由 5px 增至 10px（圆角 5px），里程碑进度条高度由 6px 增至 12px（圆角 6px）；
   - **排版对齐设计图**：彻底移除末尾多余无意义的 `>` 箭头符号；排版调整为上方游戏名、下方左侧时长与右侧占比对齐，底部通栏铺满加粗进度条。
9. **正在游玩卡片多游戏轮播平滑交叉溶解过渡**：
   - 彻底解决多个游戏运行轮播切换时画面瞬间跳变的生硬感；
   - 引入硬件加速 Storyboard 柔和溶解过渡（180ms 淡出至 0.15、更新数据、220ms 平滑淡入至 1.0），过渡优雅丝滑。

### 要重点验证什么
| # | 操作 | 预期 |
|---|---|---|
| 1 | **今日统计与本周统计** | 观察卡片布局：左侧圆形日历徽章、中大字号 `Xh Ym`、右侧上下排列的变动比率与「较昨日/较上周」（无胶囊背景）。 |
| 2 | **右侧卡片顺序** | 右侧上方为「同步状态」，下方为「今日游戏」。 |
| 3 | **今日游戏封面与展开** | 游戏封面为 52×52 正方形与整行高度对齐；当游玩游戏 > 3 个时右上角显示「查看全部 ∨」，点击就地展开/折叠。 |
| 4 | **里程碑当前段位** | 进度条下方段位称号显示 Emoji（例如 `🎮 Lv.1 愿望单收集家`）。 |
| 5 | **深色模式热力图** | 切换至深色模式，热力图格子无多余描边，各阶色阶符合淡紫/紫蓝阶梯。 |
| 6 | **Hero 背景图清晰度** | 运行有封面游戏时，Hero 卡片背景图比之前更清晰可见。 |

---

## v0.2.21-alpha.6 — 2026-09-19

**验证状态**：✅ **代码已加固并通过全量 141 项单元测试与 0 警告编译**
**核心功能与优化**：
1. **跨日时间戳设置（支持自由设置 24~30 点整点）**：
   - 为避免深夜游戏跨午夜导致时长被分割，或者用户认知中“睡前都算今天”与 0 点分割的冲突，设置页新增「跨日时间戳设置」卡片；
   - 支持从 24:00 (次日 00:00 - 标准跨日，默认) 到 30:00 (次日 06:00) 自由选择；
   - 例如选择 28 点（次日 04:00），则凌晨 00:00 ~ 03:59 期间游玩的时间均自动归属于前一日，不跨日割裂；跨越 04:00 时精准分割；
   - 主页“今日游戏”与系统托盘同步遵循该跨日结算点。
2. **Notion 同步策略调整（取消空转周期同步，保留启动同步与退出同步）**：
   - 彻底取消原每 15 分钟的后台无条件空转轮询，无游戏运行时不再无意义请求 Notion；
   - 启动应用时：执行一次完整双向同步；
   - 游戏退出时：会话结束时自动触发将待同步数据上传至 Notion，保证成果即时归档；
   - 本地 5 秒进程检测与 SQLite 存储完全不受影响。
3. **日志与状态语义纠正**：
   - 本地平台检索日志标签由 `[游戏库]` 纠正为 `[平台检索]`，并将 `共检索到 X 个已安装游戏` 纠正为 `共检索到 X 个已记录游戏`；
   - Notion 同步日志中将泛化易混淆的 `游戏库` 明确拆分为 `[游戏总表]` 与 `[每日时长表]`。

### 要重点验证什么
| # | 操作 | 预期 |
|---|---|---|
| 1 | **设置页配置跨日时间戳** | 在「设置」中将跨日时间戳切换为 28:00 (次日 04:00)，重启或继续运行，设置正常持久化。 |
| 2 | **深夜跨日时长归属** | 模拟凌晨 1:00 ~ 3:00 游玩，记录正确计入前一天日期，不再割裂到今天。 |
| 3 | **退出游戏自动同步** | 玩完游戏关闭进程，观察日志与界面，数据自动推送到 Notion 每日时长表，无需手动点同步。 |
| 4 | **无游戏时不空转** | 不玩游戏时挂机后台，观察日志，不再频繁打印轮询日志。 |
| 5 | **日志语义检查** | 启动日志显示 `[平台检索] 共检索到 X 个已记录游戏`，Notion 同步清晰显示 `[游戏总表]` 和 `[每日时长表]`。 |

---

## v0.2.21-alpha.5 — 2026-09-19

**验证状态**：✅ **代码已加固并通过全量 127 项单元测试与 0 警告编译**
**核心修复**：
1. **支持 Notion 侧直接修改记录日期与关联（彻底解决改日期导致同步失败与未绑定）**：
   - **旧代码死板缺陷**：旧版 `UpsertDailySummaryFromNotionAsync` 在按 `notion_page_id` 匹配到已有行时，执行的 SQL 更新中**漏写了 `date = @date` 字段**！且如果用户改了日期或关联导致目标槽位被占，直接抛出 `SQLite Error 19: UNIQUE constraint failed: daily_summary.date, daily_summary.game_id`，同步直接崩溃中断，关联关系无法保存，导致游戏在映射库中一直停留在「● 未绑定」；
   - **全面重构动态日期感知与槽位碰撞合并机制**：
     - 若用户在 Notion 中改了日期，且目标日期空闲：**原地更新 `date = @date, game_id = @targetGameId`**，本地日期即时跟随 Notion 对齐；
     - 若用户在 Notion 中改了日期，且目标日期已有该游戏的未绑定记录或老记录：**安全合并更新目标槽位，并物理删除旧日期的孤儿行**，100% 杜绝 UNIQUE 冲突崩溃；
     - 游戏关联关系顺利刷新，未绑定状态彻底解除！
2. **彻底锁死历史记录的时长回推**：杜绝「森林之子」「pico2」「以撒」等历史记录被循环改写时长与标题的毁灭性 Bug；
3. **根除同日多条目无限累加死循环**：遇到同日多 Notion 页面跳过叠加并告警，杜绝 SQLite 内部数值无休止膨胀；
4. **远端 Notion 历史数据 100% 权威与自愈**：QA 在 Notion 网页端把时长改回正确值后，本地拉取立即遵从远端数值并自愈，绝不以本地缓存覆盖远端。

### 事故原委与深层复盘

#### 1. 为什么用户在 Notion 改了日期后，会一直同步失败并显示「未绑定」？
- **代码把本地数据库写太死了**：
  在 `SqliteRepository.cs` 的 `UpsertDailySummaryFromNotionAsync` 中：
  1. 旧代码更新已有记录时只更新了 `duration_seconds, duration_minutes, notion_page_id, game_id`，**根本没有写 `date = @date`**。这意味着只要用户在 Notion 改了日期，本地永远留在旧日期；
  2. 即使尝试写入，因为 SQLite 表上有 `UNIQUE(date, game_id)` 唯一索引，只要目标日期的槽位在本地已有行（例如老记录或幽灵记录），直接触发 SQLite Error 19 崩溃抛错；
  3. 一旦该条记录同步抛错中断，后续的关联关系映射与收尾流程被跳过，导致这几款游戏在映射库里一直处于「● 未绑定」或「同步失败」。
- **解决方案**：
  本次重构引入了全套双键查询（同时查 PageId 和目标 Date+GameId）与智能槽位裁决：
  - 遇到日期变动碰撞时，自动将权威数据合入目标行，并执行 `DELETE FROM daily_summary WHERE id = @oldId` 移除旧日期的冗余行；
  - 彻底解放数据库限制，用户无论在 Notion 怎样改日期、换关联，本地都能丝滑平移，绝不崩。

#### 2. 为什么 16:49 之后森林之子被改了 5 次单日时长？
- **触发源**：QA 在 Notion 里跨日游玩忘记改前一天日期，导致 2025-01-03 存在两个「森林之子」页面（`page-b10e` 6.3h 与 `page-a141` 6.3h）。
- **复合恶性循环链条**：
  1. **旧代码在 Pull 遇到同日多页面时**，执行了 `conflictMinutes + item.DurationMinutes`，每次同步都把第二个页面的 6.3h 累加到 SQLite 行上（6.3h → 12.6h → 18.9h...）；
  2. **Pull 判定 `localAhead` 时**，错误地对所有历史记录生效（`SQLite 12.6h > Notion 6.3h`），将该历史记录标记为 `sync_status = 'pending'`；
  3. **`SyncPendingDailyRecordsAsync` 上传链路**原本默认带 `writeDuration: true`，见到 `pending` 便把累加膨胀后的时长与新拼标题（`森林之子 · 12.6 h`、`18.9 h`）回推到了 Notion 的第一个页面！
  4. 每经过一次同步（17:09、17:24、17:40、17:57、17:59），累加便发生一次，回推也发生一次，**整整被改写了 5 次**！连带同日双页面的「pico2」「以撒」也在 17:57 和 17:59 被改写了 2 次。

#### 3. 本次加固的三重绝对锁死机制
1. **第一重锁（上传铁律锁）**：
   - 历史日期（早于 7 天前）的记录，Notion 上的时长与日期是**绝对权威**！
   - `SyncPendingDailyRecordsAsync` 检测到历史日期已有 `notion_page_id` 时，**物理跳过上传，绝不向 Notion 发送任何改写请求**，直接在本地置为 `synced`。
   - `UpdateDailyRecordAsync` 的 `writeDuration` 默认值**永久锁死为 false**。
2. **第二重锁（拉取防累加锁）**：
   - 当检测到同一天同一个游戏存在多个 Notion 页面时，**跳过同日重复页面的本地累加**，仅记录警告日志，绝不再把时长无休止往上加。
3. **第三重锁（权威覆盖与自愈锁）**：
   - 历史记录在拉取时，**一律以 Notion 远端数值为准**，绝不进行 `Math.Max` 取本地旧值，绝不判定 `localAhead`，绝不标记 `pending`！
   - **自愈效果**：QA 在 Notion 网页端把「森林之子」改回 6.3h，启动程序拉取时，本地 SQLite 的 37.8h 会立即被纠正回 6.3h，且状态为已同步，绝不反弹覆盖！

### 要重点验证什么
| # | 操作 | 预期 |
|---|---|---|
| 1 | **验证 QA 那三条改了日期的游戏** | 触发拉取同步后，**不再报错**，本地日期正确更新对齐，**未绑定状态立刻解除**！ |
| 2 | 在 Notion 网页端把「森林之子」改回 6.3h，触发程序同步 | **程序绝不再去修改 Notion 上的 6.3h**，本地时长自愈恢复为 6.3h，日志提示同步已是最新！ |
| 3 | 查看日志 | **绝不再出现「已同步 1/3 条记录」去改写历史记录**，不会再有任何单日时长被篡改！ |
| 4 | 待处理页忽略与恢复 | 忽略即时支持撤销；下方「已忽略的游戏」支持一键恢复到待处理。 |

---

## v0.2.21-alpha.4 — 2026-09-19

**验证状态**：✅ **代码已加固并通过全量 123 项单元测试与 0 警告编译**
**相对 alpha.3 的变更**：
1. 修掉「英灵神殿 5 条 SQLite Error 19: UNIQUE constraint failed: daily_summary.date, daily_summary.game_id」迁移冲突报错；
2. 待处理页新增「已忽略游戏管理与反悔药（撤销 / 一键恢复到待处理）」。

### 问题与改动说明

#### 1. 为什么英灵神殿会有 5 条 UNIQUE constraint failed 报错？
- **不是在待处理里忽略导致的**。
- **真正原因**：QA 在 Notion 里把之前因为手写标题（「英灵神殿2.4h」「英灵神殿2.5h」等）遗留的 5 条幽灵记录关联到了正规英灵神殿总表。本地拉取时触发了**记录归属迁移（Migration）**。
- 但是正规英灵神殿在这些日期里**早就已经有一行记录了**！旧代码在迁移时直接执行 `UPDATE daily_summary SET game_id = 目标游戏`，导致与目标游戏当天的已有行发生主键冲突（SQLite UNIQUE 约束冲突）。
- **修法**：在迁移前检查目标游戏当天是否已存在记录；若存在，自动将时长合并到目标行，并安全清理幽灵记录旧行，不再报 SQLite Error 19。

#### 2. 新增「忽略反悔药」
- **即时撤销**：在待处理页点击「忽略」某款游戏后，上方通知栏即时弹出 **「撤销」** 按钮，点一下立刻撤销忽略；
- **忽略面板**：若存在已忽略的游戏，待处理页下方自动出现 **「▸ 已忽略的游戏 (N)」** 折叠卡片；
- **一键恢复**：展开卡片后可查看所有被忽略的游戏，点击 **「恢复到待处理」** 即可随时将它们移回待处理队列。

### 要重点验证什么
| # | 操作 | 预期 |
|---|---|---|
| 1 | 再次触发同步或拉取 | **英灵神殿不再报 5 条 UNIQUE constraint 错误**，818 条记录全部成功同步！ |
| 2 | 待处理页中点击某个游戏的「忽略」 | 立即移入已忽略列表，通知栏弹出「撤销」按钮 |
| 3 | 展开待处理页下方的「已忽略的游戏」点击「恢复到待处理」 | 游戏立刻重回待处理列表，可正常重新绑定 |

---

## v0.2.21-alpha.3 — 2026-09-19

**验证状态**：✅ **代码已加固并通过全量 122 项单元测试与编译**
**相对 alpha.2 的变更**：彻底修掉并阻断「同一天多个条目导致 Notion 远端历史记录被篡改（英灵神殿、以撒等）」的核心事故

### 事故根因与技术全景
1. **QA 自查确认**：QA 跨日游玩时忘记把时间改回前一天，导致同一个游戏在 Notion 同一天录入了两个条目（如 9.17 录了 2.2h 和 2.4h 两个页面）。
2. **本地主键与认领冲突**：本地 SQLite `daily_summary` 有 `UNIQUE(date, game_id)` 约束（每日汇总一行），而旧版拉取同步逻辑使用 `WHERE notion_page_id = @pageId OR (date = @date AND game_id = @gameId)`。后一条记录直接将前一条记录的 `notion_page_id` 覆盖掉，导致 pageId 错位绑定。
3. **回刷与回填链路带时长覆盖**：旧代码在回填 relation / 回刷标题时，误将本地时长与重拼标题推给 Notion，导致用后一条的时长覆写了前一条的 Notion 页面。

### 加固措施（双重绝对安全防线）
1. **拉取认领唯一性与同日多条目防御**：
   - 严格以 `notion_page_id = @pageId` 精确查找；
   - 只有未绑定 Notion Page 的本地行才允许匹配认领；
   - 若检测到同一天同一个游戏存在多个 Notion 页面，本地累加游玩总时长，**绝不覆盖已绑定的 `notion_page_id` 与标题快照**。
2. **回刷与回填 100% 只读时长保障**：
   - 回刷标题与回填关联时强制设置 `writeDuration: false`，物理剥离修改 Notion「单次时长」数值属性的能力；
   - 强制使用 `titleOverride` 保留 Notion 原标题中的手写时长后缀（如 ` 2.2h`），绝不用本地缓存数值重拼标题；
   - 全程绝不修改 Notion「日期」属性。

### 要重点验证什么
| # | 操作 | 预期 |
|---|---|---|
| 1 | Notion 中同一个游戏同一天录入多条记录（或跨日游玩）后拉取 | 本地正常同步与累加，不报错，不串号 |
| 2 | 点击「回刷」或总表重新映射关联 | Notion 远端页面的历史时长数值与日期**纹丝不动**，仅更新关联游戏与图标呈现 |
| 3 | 观察本地映射库与待处理列表 | 手写时长后缀（如 `英灵神殿2.4h`）能自动剥离并精准命中总表正规游戏 |

---

## v0.2.21-alpha.2 — 2026-09-19

**验证状态**：⚠️ **待验证**（= `alpha.1` 的全部改动 + 下面这一项）
**相对 alpha.1 的变更**：修掉「记录只显示到 6/11、更早的没显示」

> 上一版（`alpha.1`）如果已经打出去了也没关系 —— 这一版多修了一项，用 `alpha.2` 区分。

### 现象：QA 的每日时长表有 287 条，但程序里最多只显示到 6 月 11 日

**不是没拉到本地，也不是设了天数上限。** 记录**早就在本地库里了** ——
拉取路径没有日期过滤，287 条全都写进本地库了。

真正的原因：**首页读取数据的窗口只有 100 天，比热力图小得多**。
热力图是 52 周（364 天），而首页取数只往回拿 100 天，于是：

- 热力图左边约 3/4 永远是空的；
- 「游戏时长记录 **N** 天」也只统计窗口内的天数 —— 你看到的 **87 天**就是这么来的。

**日期完全吻合**：今天是 9/19，往前 100 天正好是 **6/11**。

代码里那段注释还写着"覆盖 84 天热力图"—— 那是热力图只有 12 周时留下的；
后来热力图扩到 52 周，这个窗口没跟着改，两边口径就对不上了。

### 改法

- 读取窗口不再写死天数，改由 `DailyAggregator.DataWindowStart()` 统一给出
  （热力图天数 + 7 天余量 = 371 天），**两边不会再各改各的**。
- 「历史」页原来只显示**最近 100 条**（同样是显示上限，不是数据缺失），
  提高到 1000 条 —— QA 的 287 条现在能全部看到。
- 新增守护测试 `DataWindowStart_ShouldCover_EntireHeatmapWindow`，
  锁住"读取窗口必须覆盖整个热力图"这个不变量。

### 要重点验证什么

| # | 操作 | 预期 |
|---|---|---|
| 1 | 换包后打开首页 | 「游戏时长记录 N 天」应**明显变大**（接近实际天数） |
| 2 | 看热力图 | 左边不再是大片空白，能一路看到去年的数据 |
| 3 | 打开「历史」页 | 能翻到 6/11 之前的记录 |
| 4 | 若仍有缺漏 | 日志搜 `跳过` —— 若出现「跳过 N 条总表已不存在的游戏」，说明那些记录指向的总表条目被删了，那是另一回事，把日志发我 |

### 已知问题

- 若本地库确实缺更早的记录（而非显示问题），日志里会有线索，请一并发我。
- 其余同上。

---

## v0.2.21-alpha.1 — 2026-09-19

**验证状态**：⚠️ **待验证**（调试包 —— 你确认可用后我去掉 `-alpha.1`，版本落成 `0.2.21`）
**相对 v0.2.20 的变更**：修掉时长单位换算错误 + 分辨率自适应 + 里程碑区块调整

> **版本号说明**：从现在起，调试中的包带 `-alpha.N`（本版是 `0.2.21-alpha.1`）。
> 你测过没问题之后，我把它去掉、落成 `0.2.21` —— 修订号在测试通过前先不落实。
> 上一版 `0.2.20` 已按这个规则确认通过。

---

### 1. 【严重】时长换算错误：旧数据被放大了 60 倍

**现象**：Notion 里某个游戏某天是 5 分钟，程序里读成了 5 小时。

**原因**：上一版把「单次时长」的单位从分钟改成小时时，
判断"这个值是分钟还是小时"用的规则是**只看数值大小**（≤24 当小时，>24 当分钟）。
旧行里存的是分钟，于是 5（分钟）落进了"当小时"那一档 → 5×60 = **300 分钟**。

**修法**：改成**从标题里的单位判定**。标题里一直带着单位，可以确定性地判：

| 标题 | 判定 | 结果 |
|---|---|---|
| `老游戏 · 0.08 h`（程序现在写的） | 小时 | 5 分钟 ✓ |
| `老游戏 · 5 min`（早期版本写的） | 分钟 | **5 分钟** ✓（这就是报错那行） |
| `老游戏 (42分)`（更早的格式） | 分钟 | 42 分钟 ✓ |
| `黑旗10.1h`（你手写的） | 小时 | 606 分钟 ✓ |
| `半条命 45min`（手写带 min） | 分钟 | 45 分钟 ✓ |
| `Half-Life 2`（认不出单位） | 按数量级猜 | 维持原逻辑 |

**已经读错的数据会自动修**：跑一轮同步时，如果发现某行**标题明确写着分钟、而本地值恰好是
远端的 60 倍**，就以 Notion 为准把它压回去（会写一条日志 `[同步] 修正历史时长膨胀：…`）。
这条判据收得很紧，四个条件同时满足才动，不会误改正常数据。

> ⚠️ **重要**：如果不修，那个被放大的 300 分钟会被程序当成"本地领先"**推回 Notion**，
> 把线上表写坏。所以这一版请**尽快换包并跑一轮同步**。

### 2. 分辨率自适应（2K / 4K）

你反馈 2K 下右边第三块被挤扁了。根因是布局按 1080p 调死：**右列固定 340 像素**，
而窗口在 2K/4K 下宽得多，于是右列显得越来越窄。

改了三处：

| 位置 | 改法 | 1080p 下 | 2K | 4K |
|---|---|---|---|---|
| 右列宽度 | 固定 340 → 取内容宽度 26%，夹在 340~460 | **仍是 340**（无变化） | ~395 | 460 |
| 内容宽度 | 加左右对称留白，把内容限制在约 1520~1840 内居中 | 不变 | 两侧留白 520 | 两侧留白 1000 |
| 初始窗口 | 固定 1200×780 → 工作区的 62%×78%，夹在 1200×780~1720×1040 | **仍是 1200×780** | 1587×1040 | 1720×1040 |

另外**热力图格子会跟着变宽**：以前 52 周固定 16px（合计 832px），
屏幕宽时就是一条小色带；现在按卡片可用宽度在 16~26px 之间自适应，宽屏下正好铺满。
卡片放不下时维持 16px 并横向滚动 —— **和 1080p 下的表现完全一致**。

> 说明：我只在你给的截图和代码层面判断，**没有 2K/4K 的实机**。
> 如果你看到的"挤压"是别的地方（比如某个具体卡片内部），把整屏截图发我。

### 3. 里程碑区块调整

- 删掉进度条下方那句「保持热爱，让每次游玩都有迹可循。」
- 把当前段位（如 `Lv.1 愿望单收集家`）从**右上角挪到进度条下方**
  —— 它原来和「下一个里程碑：6 天 (Lv.2)」挤在同一排，窄列下会互相压。

### 要重点验证什么

| # | 操作 | 预期 |
|---|---|---|
| 1 | **换包后跑一轮同步**，然后看日志 | 出现 `[同步] 修正历史时长膨胀：…` 的修正记录 |
| 2 | 打开 Notion 每日时长表，抽查几行 | 「单次时长」数值恢复正常（5 分钟就是 `0.08 h`，不再是 5） |
| 3 | 看首页「连续游玩里程碑」卡片 | 进度条下方显示当前段位；那句"保持热爱…"已消失；右上角不再有段位文字 |
| 4 | 你的 1080p 上确认样式没变 | 布局应与之前完全一致 |
| 5 | （若你能在 2K/4K 上看） | 右列不再被挤扁；热力图铺满卡片宽度 |
| 6 | 关界面 → 托盘打开，连做 10 次 | 不白屏、不闪退（沿用上一版的修复） |

### 已知问题

- **2K/4K 我无法实机验证** —— 上面第 5 项需要你或 QA 看一眼。
- "关界面 → 托盘打开"的崩溃修复仍待你实测确认（上一版你没机会测，因为包起不来）。
- 其余遗留项同上。

---

## v0.2.20 — 2026-09-19

**验证状态**：✅ **你已确认测试通过**
**相对上一版**：修掉「一启动就崩」；版本号改按 SemVer 命名

> ### 🗑️ 请丢弃 `v0.9.5-alpha.23` 那个包
>
> 它**根本起不来** —— 双击后 1 秒内闪退。你看到的是白屏一闪就没，
> 日志里只有一行：
>
> ```
> XamlParseException: Cannot locate resource from
> 'ms-appx:///Microsoft.UI.Xaml/Themes/themeresources.xaml'
> ```
>
> 不是你的环境问题，是那个包本身是坏的。这一版已修好。

### 为什么上一版启动就崩

我把 WinUI 的依赖从「一个元包」改成了「6 个子包」，想省掉程序用不到的
AI / ML / 搜索 / 小组件（约 55MB）。**结果应用 PRI 不再合并框架的 PRI** ——
WinUI 的主题资源（`themeresources.xaml`）就在里面，合并丢了就找不到，程序直接崩。

对比同一次发布的 `GameTimeTracker.App.pri`：

| 依赖写法 | App.pri 体积 |
|---|---|
| 元包（原样） | **2,227,376 字节**（已合并主题资源） |
| 6 个子包（我改的） | **43,464 字节**（只剩本程序自己的资源） |

**为什么之前几版没暴露**：这个坏 PRI 被增量编译掩盖了 —— `bin/` 里一直留着
早先构建的合并版 PRI，后续构建直接沿用，所以 alpha18~alpha22 的包都是好的。
直到我为了排除干扰跑了一次全新编译，PRI 才按"未合并"重新生成，于是
alpha.23 带上了坏 PRI。

**已回退**：依赖改回 `Microsoft.WindowsAppSDK` 元包。省这 55MB 不值得。
并在 `publish.ps1` 里加了一道**护栏**：`GameTimeTracker.App.pri` 小于 1MB
就直接中止打包，不会再产出这种"能生成但起不来"的包。

### 版本号命名改了

从这一版起按 **SemVer** 命名，不再用 `alpha23` 那种写法：

- 本版 = **`0.2.20`**
- 初始开发阶段（`0.x`）约定：**每个交付包递增 PATCH** —— `0.2.20` → `0.2.21` → …
  这样每版都能对得上号，出问题能说清是哪一版
- 预发布后缀（`-alpha.1` / `-beta.1`）平时留空，需要时再填
- 规则细节见 README「版本号规则」

包名相应变成 `GameTimeTracker-v0.2.20-win-x64.zip`，程序里显示的版本是 `v0.2.20`。

### 这一版同时包含上一版（alpha.23）的代码改动

alpha.23 虽然包是坏的，但里面的**代码修复是有效的**，已包含在本版：

- **关界面时不再拆掉页面** —— 这是「白屏 / 闪退」的机制来源（详见下节）
- 「要不要重建」的判断改成看内容是否还在，不再依赖会过期的 `_currentNav`
- 日志补了 `有内容=True/False` 字段，方便判断死在哪个阶段

### 要重点验证什么

| # | 操作 | 预期 |
|---|---|---|
| 1 | **双击启动** | 正常打开，不再闪退/白屏 |
| 2 | **关界面 → 托盘打开，连续做 10 次** | 每次都正常显示，不白屏、不闪退 |
| 3 | 关闭界面后**等 1~2 分钟**（让后台同步跑完）再打开 | 同上 |
| 4 | 打开日志搜 `[托盘] 恢复界面开始` | 应看到 `有内容=True`，且**没有** `重建页面` 行 |
| 5 | 关闭窗口的提示 | 只弹**一次**，不反复弹 |

### 已知问题

- 第 2、3 项对应的原始崩溃我**在本机复现不出**（我的测试环境用无效 Token，
  少了"同步成功 → 推 UI 更新"这一段，数据量也小得多），所以那个修复基于机制分析。
  如果还崩，**把整份 `data/logs/app.log` 发我**，新日志能直接区分
  "死在隐藏期间"还是"打开瞬间"。
- 其余遗留项同上一版。

---

## v0.9.5-alpha.23 — 2026-09-19

**验证状态**：🗑️ **包不可用（一启动就崩）** —— 代码改动有效，但产物坏，已被 `v0.2.20` 取代
**相对 alpha22 的变更**：**不再在隐藏时拆掉页面** —— 这是「白屏 / 闪退」的机制来源（**待你实测确认**）

> ### 版本号格式变了（SemVer）
>
> 从这一版起采用语义化版本：`0.9.5-alpha.23`（原先写作 `alpha23`）。
> 多出来的那个点不是笔误 —— SemVer 规定预发布标识与序号之间用 `.` 分隔。
> 排序规则：`alpha.2 < alpha.10`（按数字比，不是按字符串）。
> 完整的版本规则见 README「版本号规则」。

### 问题：关掉界面再从托盘打开，白屏 / 闪退

这是连着三版没修干净的那个问题。

**⚠️ 先说清楚我的信心程度**：这次的结论**我无法在本机复现验证**
（下面的「我这次实测到的情况」写了原因），它来自对你日志的机制分析。
所以请看本节的「要重点验证什么」，直接实测判断。

**先纠正前两版的错误结论**（都作废，不要再参考）：

| 版本 | 当时声称的原因 | 实际情况 |
|---|---|---|
| alpha21 | `Closing` 里同步拆 XAML 树 | ❌ 无效，你实测仍崩 |
| alpha22 | 发布时的 `ReadyToRun` | ❌ 无效。alpha22 本来就是非 R2R（`App.dll` 399KB），你实测仍崩 |

**真正的原因：程序在窗口隐藏时把页面从可视树上摘掉了。**

原逻辑（从项目基线就存在，不是这几版新加的）是：关窗口时把
`ContentFrame.Content` 置空、丢掉各页引用、并清掉图片缓存，图的是隐藏期间少占内存。

问题出在**它只摘了一半**：

- 页面被摘出可视树后，各页面的 ViewModel **仍然强引用着页面** ——
  `x:Bind` 生成的绑定会把 `PropertyChanged` 处理器挂在 ViewModel 上，
  所以 `_homePage = null` 并不能真正释放它，只是让它"脱离界面"。
- 而后台的同步与数据刷新随时会驱动那些绑定去更新元素。
  你的日志里就有证据 —— 拆完树的 60ms 后程序仍在打
  `[同步] 已从 Notion 同步 9 条记录`。
- **更新一个界面元素已经销毁的绑定，WinRT 投影层解析 ABI 指针时就会踩空。**

崩溃表象：`CoreMessagingXP.dll` / `0xc000027b` / `combase` E_FAIL / **日志一行都没有**
（原生层崩溃，托管异常处理器接不到）。

**白屏与闪退是同一个问题的两个阶段**：窗口已经被显示出来了，但进程紧接着在
重建/刷新过程中崩掉 —— 所以你看到的是"先白屏、然后闪退"。

另外，重建判断原本看的是 `_currentNav`（"上次导航到哪"）而不是"内容还在不在"，
而释放那段 `try` 会把异常吞掉 —— 一旦它在清 `_currentNav` 之前就失败，
这个状态就会留着旧值，于是重建被跳过、界面真的空着。
现在改成判断**内容本身是否还在**，这条路也堵死了。

### 补充：我这次实测到的情况

用与打包脚本相同的参数发布，跑「关窗口 → 隐藏 90 秒 → 托盘打开」3 轮：

```
13:41:19.917 [窗口] 已隐藏（保留页面，后台刷新可安全更新）
13:42:50.452 [托盘] 恢复界面开始（有内容=True _currentNav="Home" _navToRestore="Home"）
13:42:50.453 [托盘] 恢复界面结束          ← 无需重建，内容原样还在
```

新版的日志会长这样，`有内容=True` 就说明页面没被拆掉。

### 改法

**关窗口时只隐藏，不再拆页面。** 顺带把那段拆树代码整个删掉，
免得以后又被当成"内存优化"加回来。

- 代价：隐藏期间当前页面留在内存里。**这是刻意的取舍** —— 稳定性优先，
  而且页面本来就随导航常驻（只有热力图那张卡片相对重）。
- 「要不要重建」的判断改成**看内容是否真的没了**（`ContentFrame.Content is null`），
  不再依赖可能过期的 `_currentNav`。
- 日志里补了一个字段，以后一眼能看出来：

```
[托盘] 恢复界面开始（有内容=False _currentNav="" _navToRestore="Home"）
[托盘] 内容已释放，重建页面：Home
```

### 要重点验证什么

| # | 操作 | 预期 |
|---|---|---|
| 1 | **关界面 → 托盘打开，连续做 10 次** | 每次都正常显示，不白屏、不闪退 |
| 2 | 关闭界面后**等 1~2 分钟**（让后台同步跑完）再打开 | 同上 |
| 3 | 关界面后切到别的页面（如设置页）再打开 | 显示的是你离开时那个页面 |
| 4 | 打开日志搜 `[托盘] 恢复界面开始` | 应看到 `有内容=True`，且**没有** `重建页面` 行 |
| 5 | 顺带确认：关闭窗口只弹**一次**提示 | 不再反复弹 |

### 已知问题

- **我这边复现不出那个闪退**。我的环境里 Notion 是断开的（用无效 Token 以免写你的线上表），
  少了"同步成功 → 推 UI 更新"这一段；而且你的库有 800+ 条记录，负载也不同。
  所以「不再拆页面」这个改动基于机制分析，**需要你按上表第 1、2 项实测确认**。
- 如果你的日志里能再抓到一次崩溃，请**把整个 `data/logs/app.log` 发我** ——
  新版日志会告诉你它死在"隐藏期间"还是"打开瞬间"，这是下一轮定位的关键。
- 其余遗留项同 alpha22。

---

## v0.9.5-alpha22 — 2026-09-19

**验证状态**：❌ **结论已作废** —— 你实测 alpha22 仍然闪退，见 alpha.23 那节
**相对 alpha21 的变更**：~~真正修掉了「从托盘打开闪退」~~（R2R 不是原因）

> ### ⚠️ 本节结论已作废（2026-09-19 晚订正）
>
> 本节把闪退归因于发布时的 `ReadyToRun`。执行者实测 alpha22（本来就是非 R2R，
> `App.dll` 399KB）**仍然闪退**，所以该结论不成立。
> 真正的原因见上方 **alpha.23** 一节：隐藏窗口时把页面摘出了可视树。
>
> 本节保留下来只为记录排查过程，**请勿据此回退 R2R 相关设置之外的东西**。
> （`PublishReadyToRun=false` 本身可以留着 —— 它确实是让产物更稳妥的做法。）

> ### 先说清楚：alpha21 的结论是错的
>
> alpha21 声称修好了闪退，**其实没有**。原因是我定位时掉进了一个坑，见下面「我的失误」。
> 这一版用**和打包脚本完全相同的参数**重新做了对照实验，找到了真正的原因。

### 真正的原因：发布时的 ReadyToRun

崩溃的表象和 alpha21 那节描述的一致（`CoreMessagingXP.dll` / `0xc000027b` /
`combase` E_FAIL / 日志空白），**但起因不是界面释放的时机**。

同一份代码，**只改发布参数**做对照：

| 发布方式 | 结果 |
|---|---|
| **ReadyToRun 开启**（打包脚本一直走的路） | **1~3 轮之内必崩** |
| ReadyToRun 关闭 | 连续 4~8 轮全部正常 |

机理与此前 `PublishTrimmed` 那次属于同一类：WinRT 投影层要在**首次访问类型**时
解析 ABI 指针（`WinRT.TypeExtensions.GetAbiToProjectionVftblPtr`）。
ReadyToRun 会把一部分投影代码预编译、固化调用点；而本程序会**把界面树整个释放、
之后再按需重建**（收进托盘省内存的那套逻辑）—— 重建时类型初始化顺序一变，
就会踩到已失效的指针。

**修法**：

- `csproj` 里 `PublishReadyToRun` 改为**恒定 False**（原先写成条件式，Release 下是 True）
- 打包脚本显式加 `-p:PublishReadyToRun=false`（双保险，不依赖 csproj）
- 打包脚本**新增一道产物校验**：程序集体积超阈值就直接中止打包
  （开了 R2R 的 App.dll ≈ 736KB，正常 ≈ 400KB，阈值取 600KB）

> 代价：冷启动多一次 JIT，略慢一点点，换稳定。

### 顺手修的两件事（与崩溃无关）

**1. 托盘提示「老弹」**：`Shell_NotifyIcon` 用的是复用结构体，
`uFlags |= NIF_INFO` 设上后再也不清 —— 于是**弹过一次之后，每次刷新托盘图标
都会再弹一次**（而刷新很频繁：开始/结束游戏、切回窗口、定时刷新）。
已改为调用后立刻清掉。

**2. 「已最小化到系统托盘」每关一次窗口弹一遍**：改为**每次运行只弹一次**。
（就是你说"有点烦"的那条。）

### 我的失误（说明一下，免得你觉得我在挤牙膏）

前两轮我为了发布快，测试包用了 `-p:PublishReadyToRun=false`，
**恰好关掉了 ReadyToRun**；而你的打包脚本是开着的。
**我验证的环境和你的实际环境不是同一个**，所以那两轮"已修复"的结论无效 ——
你测到的自然还是崩溃。

教训：**验证必须用与交付完全相同的构建参数**，否则测的就不是同一个东西。
这一版的所有实验都用打包脚本的参数跑。

### 要重点验证什么

| # | 场景 | 预期 |
|---|---|---|
| 1 | 关界面 → 托盘打开，**连续重复 8 次** | 不再闪退（核心） |
| 2 | 关闭界面时的提示 | 只在**本次运行第一次**关闭时出现 |
| 3 | 日志里搜 `[窗口]` / `[托盘]` | 面包屑完整、顺序正确 |
| 4 | 关闭界面后看任务管理器 | 常驻内存仍会回落（省内存的优化没丢） |
| 5 | 其余场景 | 同 alpha20 的清单（热力图 / 映射库 / 时长恢复 / 记录归属） |

---

## v0.9.5-alpha21 — 2026-09-19

**验证状态**：❌ **本版的修复无效，已被 alpha22 取代**
（当时测出的"已修复"是因为我的验证包恰好关掉了 ReadyToRun，
与打包脚本的实际参数不一致，属无效验证。详见 alpha22 一节。）
**相对 alpha20 的变更**：修「从托盘打开就闪退」——原生崩溃，日志里一行都不会有

> ### 为什么要发这一版
>
> alpha20 有一个**一碰就崩**的问题：关掉界面、再从托盘点开，程序直接闪退，
> **而且日志里没有任何记录**。
>
> 我在本机做了对照复现（同样的操作时序）：
>
> | 版本 | 结果 |
> |---|---|
> | alpha20（旧） | **第 1 轮就崩**，`0xC000027B` |
> | 本版（新） | **连续 6 轮 + 另 4 轮，全部正常** |
>
> **alpha20 里那一批修复（热力图布局 / 映射库未绑定 / 时长被清零 / 记录归属）
> 在本版里同样完整包含**，可以一并用这版验证。

### 改了什么

**1. 从托盘打开会闪退（原生崩溃）**

崩溃模块 `CoreMessagingXP.dll`，异常码 `0xc000027b`（STATUS_STOWED_EXCEPTION），
WER 签名是 `combase.dll` / `80004005`（E_FAIL）。

**为什么日志是空的**：这是**原生层**崩溃，程序里那两个兜底
（`UnhandledException` 与 `AppDomain.UnhandledException`）都拦不到。

**原因**：把界面关掉时会"收进托盘"，代码顺手把页面内容释放掉省内存 ——
但这个释放是**在窗口关闭事件里同步执行**的。关闭事件处于窗口消息处理过程中，
此刻把页面置空、再强制 GC 两次并等待终结器，等于把 XAML 的界面树从框架脚下抽掉：
界面线程是 STA，终结器线程去释放这些线程亲和的 COM 对象会直接返回 E_FAIL。

**修法**：先隐藏窗口，再把这个释放动作排到后续的消息队列里执行。
顺序保持原样 —— 释放一定排在下次「打开」触发的页面重建之前。

**2. 给这条路径补了「面包屑」日志**

```
[窗口] 收到关闭请求，收进托盘
[窗口] 已隐藏，视觉树释放已入队
[窗口] 视觉树已释放完毕（隐藏期间常驻内存已回落）
[托盘] 显示主窗口…
[托盘] 主窗口已显示
```

以前原生崩溃时日志全空，根本不知道死在哪个调用；现在即使再出问题，
看最后停在哪一行就能定位。

### 要重点验证什么

| # | 场景 | 预期 |
|---|---|---|
| 1 | 关界面 → 托盘打开，**连续重复 5~6 次** | 不再闪退（这是本版的核心） |
| 2 | 从托盘打开之后 | 界面正常重建，不是白屏 |
| 3 | 日志里搜 `[窗口]` / `[托盘]` | 上面 5 行都在，顺序正确 |
| 4 | 关闭界面后看任务管理器内存 | 常驻内存应明显回落（这个优化没被去掉） |
| 5 | 其余场景 | 同 alpha20 的验证清单（热力图 / 映射库 / 时长恢复） |

### 已知问题

- 同 alpha20：拉取每日记录会为总表里的游戏在本地建行，**待产品决策**。
- 本版**未做长时间（72h）连续运行观测**。

---

## v0.9.5-alpha20 — 2026-09-19

**验证状态**：⚠️ 代码已改，**待构建验证**
**相对 alpha19 的变更**：修 4 个你实测反馈的问题（热力图布局 / 手工标题 / 时长被清零 / 识别不到已绑定的关系）

> ### 为什么要发这一版
>
> alpha19 你装了、也能跑，但暴露了 4 个问题。其中 **「时长被清零」会损坏你的数据**，
> 请尽快换到 alpha20。

### 改了什么

**1. 热力图数据一出来，右边的区块就掉到下面去了**

原因不在热力图本身，在**外层的滚动容器少设了一个开关**：
`ScrollViewer` 的「有没有滚动条」和「能不能横向滚动」是两个独立设置，
程序只设了前者。结果容器仍然按"无限宽"去测量内容，
热力图那 52 周 × 16px = 832px 就把整个页面撑宽到 ~1250px。

而页面还有个响应式判定：宽度小于 880 时把右侧那一列挪到下面。
被撑宽后这个判定就误判成"窗口很宽"，把右列挪回右侧 —— 可窗口其实还窄着，
**于是右列被推到屏幕外**。你观察到的"数据图较多时会拉伸"就是这个时点（热力图渲染出来的瞬间）。

修法：补上那个开关，并把这个响应式判定改挂在不受内容影响的容器上。

**2. 记录明明有「关联游戏」，程序里那款游戏却是「未绑定」、而且没有记录**

这是本次最要紧的一条，**是两个 bug 叠在一起**。

**先发生的事**：你（或 QA）把表的属性改了名（`游戏` → `关联游戏` 等），
但那段时间**程序还是旧版本**。旧版本按旧属性名去读，
**写入会报 400 所以你看得见，读取却不会报错 —— 它静默地读不到「关联游戏」**，
于是把每一条记录都当成"没有关联"，为它新建了一个**未绑定**的游戏行。
日志里能看到时间线：同一时段既有 `400: 游戏名称 is not a property that exists`，
又有 `已从 Notion 同步 816 条记录` —— 后者就是这次静默的批量误建。

**然后的事（更隐蔽）**：程序升级后能读到 relation 了，也为每条记录
解析出了**正确**的游戏行；但更新每日记录时只改了时长和页面 id，
**唯独没有把记录换到正确的游戏上**。结果就是：

> 记录躺在那个「未绑定」的游戏上，而**绑好关系的游戏是空的**。
> 所以你在映射库里看到一堆未绑定，同时"已绑定"的那些又没有记录。

**修法**：
- 拉取时若发现「旧行没绑定、而这次解析出的游戏绑定了关系」，就把记录**迁过去**
  （反向绝不动：旧行已绑定说明它有自己的归属，可能是你手动改过的）
- 迁移后那个行如果已经空了（未绑定 + 无记录 + 无会话 + 无可执行文件），
  判定为导入期产生的**幽灵行**，归档后清掉 —— 否则「待处理」的数字永远降不下来
- 认领本地行时，两边的名字都会先剥掉时长后缀再比 ——
  幽灵行里存的是旧版本写进去的带后缀原串（「致命躯壳2.2h」），不剥就永远对不上

> **升级后跑一轮同步即可自动修复**，不需要手工改表。
> 幽灵行会被移进 `deleted_archive` 表（可追溯），不会凭空消失。

**3. 某一行记录的时长被清成了 0（数据损坏）**

「喵门镖局 7/17」那行的标题是程序格式「喵门镖局 · 0 h」，说明它被程序重写过。
原因是**「回刷标题」这个动作无法只改标题** —— 标题串里就含时长（「游戏名 · X h」），
写标题时必然连「单次时长」属性一起写。于是拿本地的 0 去回刷，就把 Notion 上原本的时长覆盖成了 0。

修法：**时长为 0 的记录一律不参与回刷**。

> **那一行怎么办**：升级后先让它跑一轮同步。
> 如果本地还留着原来的分钟数，程序会把它重新推回 Notion（拉取取较大值、且会标记为待推送），自动恢复。
> 如果跑完还是 0，说明本地也已经是 0，只能靠 Notion 的「页面历史」手工找回。

**4. 删除对账可能误删本地记录（顺带加固）**

「删除对账」的逻辑是：本地标着"已同步"、但这一轮从 Notion 没拉到的记录，就归档并从本地删掉。
它的前提是"这一轮拉取是完整的"。而拉取会跳过**解析不出来的行**
（日期属性为空、或标题和关联游戏都为空），那些行不在结果里，
本地对应记录就会被误判成"Notion 已删除"而删掉，连带把时长一起抹掉。

修法：远端拉到的条数比本地已同步的还少一半以上时，整段跳过，宁可漏删不误删。

### 要重点验证什么

| # | 场景 | 预期 |
|---|---|---|
| 1 | 打开「今日」页，确认热力图正常显示 | 右侧那一列**不再掉到下面**；拉宽/缩窄窗口时布局正常切换 |
| 2 | 打开「游戏映射」页，对照 Notion 总表 | 有「关联游戏」的游戏应当是**已绑定且有记录**；不再出现"绑好的没记录、未绑定的却挂着记录" |
| 3 | 查「游戏映射」里那些「未绑定」 | 跑一轮同步后应减少；剩下的若确实对不上任何总表条目，才需要手工处理 |
| 4 | 查「喵门镖局 7/17」那行 | 跑一轮同步后时长应恢复；若仍是 0 请告诉我 |
| 5 | 观察「待处理」的数字 | **不再增长**，且会下降（幽灵行被清掉，进 `deleted_archive`） |
| 6 | 随便玩一个 Steam 游戏 10 秒以上 | 侧边栏出现计时；同步后 Notion 记录正常 |

### 已知问题 / 待产品决策

- **拉取每日记录会为 Notion 总表里的游戏在本地建行**。设计上这是必要的
  （每日记录必须挂到某个游戏上），但结果是：程序会把**你总表里的游戏**都导入本地，
  在「游戏映射」里显示为一堆「未绑定」。
  **这一条尚未处理，等你定方向**（是否要给这类"仅来自 Notion"的记录单独标记、在映射库里折叠或过滤）。
- 本版**未做长时间（72h）连续运行观测**。
- 其余遗留项同 alpha19。

---

## v0.9.5-alpha19 — 2026-09-18

**验证状态**：✅ 已构建并运行验证（QA 已实装；下列问题已由 alpha20 修复）
**相对 alpha18 的变更**：修复「主流平台游戏检测不到」的两处漏识别

> ### 为什么要发这一版
>
> alpha18 有一个**严重问题：主流平台的游戏可能检测不到，程序完全不计时**。
> 已知影响 Steam（QA 实测过）和 GOG / Ubisoft。
> **如果你手上是 alpha18，请换成 alpha19 再测。**

### 改了什么

**1. Steam 游戏可能整个被漏掉（QA 实测问题）**

Steam 的安装清单里有个 `StateFlags` 字段，程序之前把它当"只有等于 4 才算已安装"来判断。
但这个字段是**位标志**（多个状态叠加），不是单值。实际常见的有 `4`、`6`、`1030`。
结果：**部分已装好的游戏被当成"未安装"跳过了**，游戏不在程序的认识列表里，玩它就不会计时。

修法：改为按位判断（含"已完整安装"位、且不含"已卸载"位）。

**2. GOG / Ubisoft 的游戏一个都扫不到**

这两个平台的客户端是 **32 位程序**，在 64 位 Windows 上它们把游戏信息写在注册表的
`WOW6432Node` 分支下，而本程序是 64 位、默认只读 64 位分支 —— 于是**完全看不到游戏**。
（Steam 和 WeGame 的代码本来处理了这个问题，GOG / Ubisoft 漏了。）

修法：两个分支都读。

**3. 未绑定的游戏会每轮同步都重复推送（性能问题）**

按设计，游戏**没绑定总表也会照常推送到每日时长表**（只是不带关联、绑定状态写「未绑定」）。
这个行为是对的，但实现上有个漏洞：推送完状态被标成「未绑定」，
而"待上传"的筛选条件是"状态不等于已同步"——于是**「未绑定」永远命中**，
每轮同步都会把这条记录**重新推一遍，永远不停**。

影响：刚配好 Notion、还没绑游戏的那段时间，每 15 分钟会把所有未绑定记录全打一遍，
而且记录随天数累积，越用越慢。已修正为「未绑定」不再重复推送，
但**时长一增加就会自动重新推送**（不会漏更新）。

**4. 补齐检测过程的日志（这次排查最大的障碍）**

之前**检测环节一行日志都没有** —— 因为代码用的是 `Console.WriteLine`，
而本程序是无控制台窗口的程序，这些输出**全部被丢掉了**。
所以"游戏没被计时"时，日志里只有 Notion 的报错，看不出任何检测线索。

现在每个平台都会输出一行**扫描汇总**，包含"扫到几个 / 收录几个 / 跳过几个及原因"：

```
[游戏库] steam: 扫描 15，收录 12，跳过 3（StateFlags 非已安装(6)×2、安装目录不存在×1）
[游戏库] gog: 扫描 4，收录 4
[游戏库] 共检索到 16 个已安装游戏
```

**这行的用法**：如果某款游戏没被计时，先看对应平台那行——
- `收录` 数不含它 → 看括号里的跳过原因，就知道为什么被排除了
- 平台整行都没出现 → 那个平台没装，或检测器没认出它已安装
- 另外还有 `[计时] 开始记录「游戏名」...` / `[计时] 结束记录 ...`

**5. 顺手精简了日志噪音**

"正在刷新游戏库…""正在从 Notion 拉取每日记录…"这类**纯进度提示不再写日志**
（它们没有任何诊断价值，下一行就是结果），只保留结果与失败原因。
每轮同步因此少掉约一半的日志行。

**6. 总表「别名」带年份后缀时匹配不上（QA 实测问题）**

现象：正在玩 Valheim，总表里已有「英灵神殿」条目（别名写了 `Valheim (2020)`），
程序却把它当成新游戏、弹窗让你新建。

原因：别名末尾的 `(2020)` 没被处理，程序拿去比对时实际比的是 `valheim 2020` 而不是 `valheim`，
于是名称匹配失败、模糊相似度也只有约 74%（差一点点过不了 80% 的阈值）。

已修：比对时会剥掉**末尾括号里的年份**：
`Valheim (2020)` → `valheim`、`Doom (2016)` → `doom`、`Prey [2017]` → `prey`。

**只剥括号形式，不剥裸年份** —— `Football Manager 2024`、`F1 2023`、`NBA 2K24`
这类游戏名里的年份是有意义的，剥掉会让不同代互相误匹配，所以刻意保留。

**同时放宽了"版本后缀"**：`Deluxe / Ultimate / Definitive / Complete / Gold /
Premium / Special / Collector's / Enhanced / Anniversary Edition`、
`Game of the Year Edition`、`Remastered` 等在比对时会被忽略，
这样总表写「XX Deluxe Edition」而进程名是「XX」也能匹配上。

> 匹配**完全基于游戏名**，不要求你在总表里填 Steam AppID。
> 但「别名」建议写成 `Valheim (2020)` 这种**带括号**的年份形式
> （不写年份也可以），写成 `Valheim 2020`（裸年份）时程序无法安全地剥掉它。

**7. 每日时长表的属性改名**

按你的要求改了三个属性名（**只改每日表，总表不动**）：

| 旧名 | 新名 | 类型 |
|---|---|---|
| `游戏名称` | **`游戏动态`** | Title |
| `时长` | **`单次时长`** | Number |
| `游戏` | **`关联游戏`** | Relation → 游戏总表 |

不改的话同步会报 `400: property does not exist`。
改完记得把 Rollup 的 `Property` 那一栏也从 `时长` 改选 `单次时长`。

**8. 移除模糊匹配：只保留确定性匹配**

之前"相近但不相同"的名字会按字符串相似度被列成候选。问题是它**分不开相似但不同的游戏**，
而且分数看起来还很有说服力：

| 你在玩的 | 被列成候选的 | 相似度 |
|---|---|---|
| Portal | Portal 2 | 85.7% |
| Half-Life | Half-Life 2 | 90.0% |
| Football Manager 2023 | Football Manager 2024 | **97.6%** |
| FF VII Remake | FF VII Rebirth | 88.9% |

绑定弹窗会把这些显示成「XX (98% 匹配)」**并默认勾选第一个** ——
点一下确定就把时长记到错误的游戏上了。

**现在改为只给确定性匹配**，靠规则而不是靠猜：
- 「游戏标识」命中（若你填了）
- 名称 / 别名完全相同
- **归一化后**完全相同（抹平标点、大小写、罗马数字，并剥掉末尾括号年份与版本后缀）

匹配不上就**如实显示"暂无匹配"**，让你手动搜索或新建。
宁可让你多点一下，也不给一个看着很像、实际错误的建议。

> 副作用：`Portal` 这种和 `Portal 2` 名字很接近的情况，现在不会再被"猜"成 `Portal 2`。
> 如果确实是同一款游戏，把另一种写法加进总表的「别名」即可。

**9. 「单次时长」的单位从分钟改成小时**

`单次时长` 现在存的是**小时、保留 2 位小数**（如 `0.25` 表示 15 分钟），
不再是分钟。这样它和每日记录标题里的 `· 0.25 h` 终于是同一个单位了。

> **为什么是 2 位小数而不是 1 位**：1 位小数的粒度是 6 分钟，
> 1440 个可能的分钟值里有 **1200 个无法精确还原**（15 分 → 0.2 h → 读回 12 分，差 3 分钟），
> 还会连带引发"本地永远领先 → 每轮重复推送"。
> 2 位小数的粒度是 0.6 分钟，**1..1440 分钟全部都能精确往返**（误差 0），
> 所以 15 分 → 0.25 h → 15 分，不丢精度。这一条有穷举测试守着。

**对 Rollup 的影响**：Rollup 汇总出来的也是**小时**了。
- 你手工那列本来就是小时 → Formula 直接 `prop("游戏时长") + prop("GT 累计")`
- 你手工那列是分钟 → 写 `prop("游戏时长") + prop("GT 累计") * 60`

**历史行怎么办**：程序读取时会自动区分 —— 单日时长不可能超过 24 小时，
所以表里 > 24 的旧值会被当作分钟（旧格式）处理，不会把 42 分钟错读成 42 小时。
但**旧的 ≤ 24 的值会被当成小时**（例如某天只玩了 15 分钟）。
如果你介意，把旧行删掉即可，不影响新数据。

### 要重点验证什么

| # | 场景 | 预期 |
|---|---|---|
| 1 | **玩一个 Steam 游戏，等 10 秒以上** | 侧边栏出现正在计时的游戏；`data\logs\app.log` 里出现 `[计时] 开始记录` |
| 2 | 打开日志搜 `[游戏库]` | 应能看到每个平台的游戏数，以及总数；**数字应与你实际安装的游戏数量大致吻合** |
| 3 | 若 Steam 游戏仍不计时 | **把日志发出来** —— 现在能直接看出是"没扫到"还是"扫到了但没匹配上" |
| 4 | 有 GOG 或 Ubisoft 游戏的话，玩一个 | 应能正常计时 |
| 5 | 非主流平台的游戏（便携版 / 模拟器等） | 按设计**不会**自动识别，需手动「添加游戏」（侧边栏左下角） |
| 6 | **属性改名后**跑一轮同步 | 不报 400；每日记录正常写入；Rollup 汇总正确 |
| 7 | 玩一个游戏若干分钟后看每日记录 | `单次时长` 是**小时**（2 位小数），与标题里的 `· X h` 一致 |
| 8 | 再有新时长产生时 | 记录会被更新（单位换算不能导致"永远重推"或"漏推"） |

### 已知问题 / 不在本版范围

- 本版**未做长时间（72h）连续运行观测**。
- 「待处理」页对不使用 Notion 的用户意义不大（它列的是"已识别但未绑定"的游戏），
  且没有非 Notion 的处置选项。**待产品决策，本版未改。**
- 其余遗留项同 alpha18。

---

## v0.9.5-alpha18 — 2026-09-18

**提交**：`3c1ea37`（功能）→ `25f40ba`（修复）→ 本次打包修复
**验证状态**：✅ 已构建验证 —— 75/75 测试通过，且**打包产物已加自动校验**（见下）
**相对 alpha17 的变更**：3 项 Notion 同步需求 + 1 处同步链顺序修正

> ### ⚠️ 本版改了「每日时长表」的属性名（需手动改名）
>
> | 旧名 | 新名 | 类型 |
> |---|---|---|
> | `游戏名称` | **`游戏动态`** | Title |
> | `时长` | **`单次时长`** | Number |
> | `游戏` | **`关联游戏`** | Relation → 游戏总表 |
>
> **只改「每日时长表」，总表不用动**（总表的标题属性仍叫「游戏名称」）。
> 不改的话同步会报 `400: property does not exist`。
>
> 程序**读取**时同时认新旧名（为了能拉回改名前的历史记录），
> 但**写入**只用新名。
>
> Rollup 也要跟着调整：`Property` 那一栏要从 `时长` 改选 `单次时长`。

> ### ⚠️ 如果你手上拿到过一个「一启动就崩溃」的 alpha18 包，请丢弃它
>
> 2026-09-18 21:12 生成的第一版 alpha18 包**有严重缺陷，一启动就崩**，
> 原因是打包时误开了代码裁剪（`PublishTrimmed`），把 WinUI 的 native 组件删掉了。
> 该产物已从 `dist/` 删除。
>
> **如何分辨**：解压后看目录里有没有 `Microsoft.UI.Xaml.dll`。
> 没有就是坏包（正常包约 449 个文件 / 285MB，坏包只有约 101 个文件 / 86MB）。
>
> **已修**：csproj 里关掉裁剪，并在打包脚本里加了**产物校验** ——
> 以后缺任何关键 WinUI 组件都会直接中止打包，不会再产出这种包。

### 改了什么

**1. 每日时长表的「游戏名称」改用总表里的名字，并同步总表图标**

- 以前：标题里的游戏名取自**本地进程名**（如 `Gumballs`）。
- 现在：如果该游戏**已绑定到总表**，就用**总表条目的名字**（即 relation 指向的那个名字，
  通常是你在总表里改好的中文名，如 `不思议迷宫`）。
- 同时把总表条目的 **page icon** 一并写到那条每日记录页面上。
- **未绑定总表**时行为不变：仍用进程名，不写图标。
- **时长后缀保留**：标题仍是 `不思议迷宫 · 0.7 h` 这种格式。

**2. 总表改名 / 补图标后，已推送的记录会跟着更新**

- 以前：在总表里把游戏改名后，**历史每日记录的标题永远是旧名字**（因为拉取只同步时长，不碰标题）。
- 现在：每轮同步会检查已推送的记录，标题或图标与总表不一致时就更新过来。
- 为控制请求量，本地存了一份「远端标题快照」做比对，**只有真的不一致才发请求**。

**3. 新用户首次保存 Notion 配置后自动同步一次**

- 以前：填完 Token 和两个数据库 ID 点保存后，要等最长 15 分钟才会看到数据。
- 现在：**从「未配置」变成「已配置」的那一刻**会自动跑一次完整同步，界面有文字提示。
- 已经配置好的用户反复点保存**不会**重复触发。

**4. 修正一处同步顺序问题**

- 「补齐总表 page icon」这一步原本排在「回填每日记录关联」之后，
  导致每款游戏**第一次**回填时拿不到图标，要等下一轮同步才出现。
- 现已把顺序调正，图标会在第一次就带上。

### 要重点验证什么

| # | 场景 | 预期 |
|---|---|---|
| 1 | 已绑定总表的游戏，在总表里把名字改成中文，等一轮同步或点「立即同步」 | 该游戏**历史**每日记录的标题变成新中文名，后缀时长不变 |
| 2 | 同上，并给总表条目设置 page icon | 历史每日记录页面的图标也跟着变 |
| 3 | 未绑定总表的游戏 | 标题仍是进程名，**不出现**图标（不应乱猜一个） |
| 4 | **重复点「立即同步」几次，观察同步耗时** | 第二次之后应**几乎瞬间完成**；没有变化时**不应**产生大量 Notion 请求。同步时间不应随历史记录数明显变长 —— 这是本版最容易出问题的地方，如果每次都跑很久，说明去重比对失效了，请报出来 |
| 5 | 全新环境：清空 `settings` 里的三个 Notion 配置项后重新填写并保存 | 保存后**自动**开始同步，有文字提示；已配置状态下再点保存不会重复触发 |
| 6 | 升级自 alpha17 的旧数据库 | 首次同步会补齐标题（一次性），之后轮次不再重复请求 |
| 7 | 在 Notion 里把某条每日记录删掉，回程序看 | 本地记录跟随清理（此行为 alpha17 已有，确认没被改坏） |

### 已知问题 / 不在本版范围

- 本版**未做长时间（72h）连续运行观测**。
- 遗留疑点（非本版引入）：静态图片保活列表长期增长、WAL 膨胀、
  `error` 状态的记录无限重试、`_activeSessions` 上 SemaphoreSlim 与 lock 混用。
- Python 遗留线（`tracker/`）已冻结，不在范围内。

---

## v0.9.5-alpha17 — 2026-09-18（基线）

**提交**：`d01cae5`（版本号相关提交 `9ad2b53`）
**验证状态**：✅ 已构建验证 —— Release 构建 0 警告 0 错误，**69/69 测试通过**

### 改了什么

- 这是**版本号体系的起点**。此前包名叫 `v1.2.1`，但代码里从未体现过版本号。
- 新增 `Directory.Build.props` 统一管理版本；设置页左下角开始显示 `GameTimeTracker v0.9.5-alpha17`。
- 初始化 git 仓库。

### 说明

- QA 若手上有更早的包（`v1.2.1` 等），它们**没有内置版本号**，
  只能靠 exe 文件属性或包名区分，请以本表为准。

---

## 排查问题时的建议

QA 报问题时，请附上：

1. **包名里的版本号**（如 `alpha18`），或程序设置页左下角显示的版本号。
2. **`VERSION.txt` 里的 Commit 短 hash**（如果包里带了）—— 这能精确定位到代码。
3. **日志**：`<程序目录>\data\logs\app.log`（超过 2MB 会轮转为 `app.old.log`）。
4. **复现步骤**：尤其是「在 Notion 里做了什么操作」和「程序当时是不是在跑同步」。

> 注意：`app.log` 里会包含游戏名、窗口标题等本机信息；如果担心隐私，
> 可只截取相关时间段，不必整个文件发出来。
